# Architecture Decision Records

**Generated. Do not edit this file.** `scripts/generate_adr_index.py` writes it from the ADRs
themselves; `tests/test_adr_index.py` fails when the two disagree — a new ADR, a renamed title, a
changed status, a reworded reversal condition.

```bash
uv run python scripts/generate_adr_index.py            # rewrite this page
uv run python scripts/generate_adr_index.py --check    # exit 1 if it is stale
uv run pytest tests/test_adr_index.py
```

**Three of these facts are read out of the files and two are declared, and the
difference matters when you are deciding what to trust.** The **title**, the **status**
and the **`## Decision` / reversal-condition structure** are parsed from each ADR, so
they cannot drift: nothing stores them twice. The **phase** and every reversal
**reading** cannot be derived from the files — they are declarations in
`scripts/adr_index.py`, each carrying the commit or the measurement that proves it, and
each locked by a totality assertion that refuses to render when the ADR set moves
underneath it.

> **Why the phase is declared and not derived.** The derivation is real —
> `git rev-list --ancestry-path <adding commit>..origin/main | tail -1` lands on a merge
> whose branch names the phase, and the sha beside each phase below is that merge. But
> CI's `test` job checks out at `actions/checkout@v4`'s default `fetch-depth: 1`, so
> that history is not there when the lock runs. A column that degrades quietly under a
> shallow clone is worse than one that is declared and cross-checked where git can
> answer.

## All 20 ADRs

| # | decision | phase | status | `## Decision` sections | reversal conditions |
|---|---|---|---|---|---|
| [0001](0001-dual-target-versions.md) | Dual-target version pinning | F1.1 (`707f8c2`) | **no `## Status`** | 1 | **none stated** |
| [0002](0002-two-layer-topology.md) | Two-layer topology (extraction vs transformation) | F1.1 (`707f8c2`) | **no `## Status`** | 1 | **none stated** |
| [0003](0003-cnpj-extraction-layer.md) | CNPJ extraction layer (WebDAV → UC Volume) | F1.1 (`707f8c2`) | **no `## Status`** | 1 | **none stated** |
| [0004](0004-pyspark-optional-extra.md) | pyspark/delta-spark as an optional extra | F1.2 (`f55ed48`) | Accepted | 1 | **none stated** |
| [0005](0005-csv-multiline-parallelism-ceiling.md) | read RFB CSVs with multiLine, accepting a file-level parallelism ceiling | F1.3 (`21bd469`) | Accepted | 1 | **none stated** |
| [0006](0006-bronze-dq-gate-policy.md) | bronze DQ gate: keep fail-closed on any reject, add a triage path, and — measured — keep it | F1.3 (`21bd469`) | Accepted, qualified | 2 | 3 |
| [0007](0007-multipart-upload.md) | adopt the SDK's multipart upload path, and pin to the minor | F1.4 (`b3cbbba`) | Accepted | 1 | **none stated** |
| [0008](0008-pii-masking-socios.md) | mask the partner NAMES in `socios`, before the first row lands | F1.4b (`5f53c49`) | Accepted, qualified | 1 | **none stated** |
| [0009](0009-deployed-revision-provenance.md) | a run must name the revision it expects, and the wheel must say what it was built from | F1.4b PR B (`44018ad`) | Accepted, qualified | 1 | **none stated** |
| [0010](0010-observation-ledger-over-a-lossy-extract.md) | derive an observation ledger, because our extract is lossy and Data Vault assumes it is not | F2 wave 1 (`4a79bfd`) | Accepted, qualified | 1 | 5 |
| [0011](0011-no-hub-socio-and-a-disappearance-driven-effectivity-satellite.md) | there is no `hub_socio`, and the effectivity satellite is driven by disappearance rather than by replacement | F2 wave 1 (`4a79bfd`) | Accepted, qualified | 1 | 5 |
| [0012](0012-two-spellings-of-the-business-key-hash.md) | the business-key hash has two spellings on purpose, and the JDK's Unicode version is now a vault input | F2 wave 1 (`4a79bfd`) | Accepted, qualified | 1 | 3 |
| [0013](0013-two-satellites-on-hub-estabelecimento.md) | two satellites on `hub_estabelecimento`, split on measured change rate, and the sharper boundary that was not taken | F2 wave 1 (`4a79bfd`) | Accepted, qualified | 1 | 3 |
| [0014](0014-dim-company-at-empresa-grain.md) | the star is grained on `empresa`, and the spec's `dim_merchant` cannot be joined to | F3 (`abee2bb`) | Accepted, qualified | 1 | 3 |
| [0015](0015-as-of-known-time-and-append-only-scd2.md) | the star answers as of KNOWN time, and its SCD2 is append-only | F3 (`abee2bb`) | Accepted, qualified | 1 | 4 |
| [0016](0016-fx-resolved-by-publication-instant-not-a-holiday-calendar.md) | the FX rate is resolved by PUBLICATION INSTANT from the PTAX series, never from a holiday calendar | F-API (`0054df1`) | Accepted, qualified | 6 | **none stated** |
| [0017](0017-a-postgres-snapshot-diff-and-the-key-that-is-not-in-the-payload.md) | a Postgres snapshot-diff, and the key you must diff on is not in the payload | F-DB (`43876b3`) | Accepted, qualified | 6 | **none stated** |
| [0018](0018-dataops-derives-it-does-not-instrument-and-it-does-not-act.md) | DataOps derives; it does not instrument, and it does not act | F4 (`3bd2f52`) | Accepted, qualified | 7 | 1 |
| [0019](0019-the-proof-runs-where-a-process-can-be-killed.md) | The exactly-once proof runs where a process can be killed; the broker is a recorded run | F5 (`5d769a3`) | Accepted, qualified | 8 | 8 |
| [0020](0020-the-triage-accelerator-is-deterministic-and-the-model-runs-as-the-control.md) | The triage accelerator is deterministic, and the model runs as the control | F6 (`ef123ac`) | Accepted, qualified | 6 | 6 |

## The numbered decisions

The **6** ADRs whose decisions are numbered, from their own headings. The reversal table below cites them by number.

**[0006](0006-bronze-dq-gate-policy.md) — bronze DQ gate: keep fail-closed on any reject, add a triage path, and — measured — keep it**

- Decision 3, resolved 2026-08-03 (F1.4b PR B) — no rate threshold

**[0016](0016-fx-resolved-by-publication-instant-not-a-holiday-calendar.md) — the FX rate is resolved by PUBLICATION INSTANT from the PTAX series, never from a holiday calendar**

- Decision 1 — the rate is the most recent quote whose PUBLICATION precedes the payment
- Decision 2 — `dataHoraCotacao` is read as BRASÍLIA TIME (UTC−03:00)
- Decision 3 — the zone lives IN THE VALUE, because `to_utc_timestamp` does not cancel the session zone
- Decision 4 — THREE deviations from §4.3's column list, where the code says two
- Decision 5 — this star carries TWO "as of" semantics on one fact row
- Decision 6 — the bronze→gold seam, inherited rather than taken here

**[0017](0017-a-postgres-snapshot-diff-and-the-key-that-is-not-in-the-payload.md) — a Postgres snapshot-diff, and the key you must diff on is not in the payload**

- Decision 1 — the extractor runs host-side, and the reason is topology, not egress
- Decision 2 — land FULL snapshots; derive the diff in the lakehouse
- Decision 3 — one `REPEATABLE READ READ ONLY` transaction, stamped by its FIRST statement
- Decision 4 — Postgres renders its own values, under GUCs that are pinned AND read back
- Decision 5 — `hub_merchant` keys on its OWN identifier, and the CNPJ is reached by a LINK
- Decision 6 — the snapshot axis is declared on `BronzeTable`, and `_snapshot_at` is a CONTRACT column

**[0018](0018-dataops-derives-it-does-not-instrument-and-it-does-not-act.md) — DataOps derives; it does not instrument, and it does not act**

- Decision 1 — telemetry is a VIEW over system tables; the pipeline is not instrumented
- Decision 2 — freshness is two metrics, and neither may exist without a declared cadence
- Decision 3 — the reconciliation REPORTS, prints the remedy, and never acts
- Decision 4 — the reclaim reaches the triage path, and the proof is repaired in the same change
- Decision 5 — no per-reason DQ tolerance, and the measurement that was missing ships instead
- Decision 6 — governance is ALL imperative; nothing goes in the bundle
- Decision 7 — the performance case study leads with what did not move, and its own prediction is falsified

**[0019](0019-the-proof-runs-where-a-process-can-be-killed.md) — The exactly-once proof runs where a process can be killed; the broker is a recorded run**

- Decision 1 — the spec's parenthetical is RETRACTED; the blocker was the NAT, not the platform
- Decision 2 — the exactly-once proof runs LOCAL, and it stays local
- Decision 3 — ONE corpus, TWO transports
- Decision 4 — the processing identity is the DELIVERY COORDINATE, not the payment id
- Decision 5 — the dedup key is the identity ALONE, and one watermark arm is not a measurement
- Decision 6 — the managed broker is a RECORDED RUN, not a standing service
- Decision 7 — only the PASSWORD is a secret; a host and a username are coordinates
- Decision 8 — where the instrument cannot be READ, the run says so rather than assuming its value

**[0020](0020-the-triage-accelerator-is-deterministic-and-the-model-runs-as-the-control.md) — The triage accelerator is deterministic, and the model runs as the control**

- Decision 1 — the classifier is deterministic, and the model is NOT it
- Decision 2 — the model runs anyway, as the NEGATIVE CONTROL
- Decision 3 — the agent writes no table
- Decision 4 — the agent emits an issue; a separate publisher opens it, and the boundary is a DIRECTORY
- Decision 5 — blast radius is a DECLARED manifest, locked against the registries
- Decision 6 — the comparison horizon is bounded, and the agent says when N was not available

## Reversal conditions

**41 conditions, stated by 10 of the 20 ADRs. 10 have been read** (3 LOOKS MET, IS NOT, 2 MET, 4 NOT MET, 1 UNCLOSABLE); the remaining 31 are marked `NOT READ`, which is not `NOT MET`. **10 ADRs state no reversal condition at all** — 0001, 0002, 0003, 0004, 0005, 0007, 0008, 0009, 0016, 0017 — and that is a finding rather than an omission of this page's.

`MET` and `NOT MET` are measurements. **`LOOKS MET, IS NOT` is the state this table exists for**: something arrived that resembles the condition and does not satisfy it, which collapsed into a boolean reads as `MET` — and that is how a decision gets reversed by a resemblance. `UNCLOSABLE` means no change to this repository can ever close it. `NOT READ` means nobody has looked.

| ADR | condition, as the ADR states it | state | read | on what |
|---|---|---|---|---|
| [0006](0006-bronze-dq-gate-policy.md) | **Per-reason counts that are not first-match-wins**, so a tolerated reason cannot shadow a never-tolerated one. Structural; no quantity of months substitutes for it. | **MET** | 2026-08-18 | `src/opl/bronze/rule_overlap.py` shipped in F4 Task 3 — one aggregate pass per (table, batch) counting every rule independently, over every registered contract. ADR 0006's own `Where the three stand` item 1 |
| [0006](0006-bronze-dq-gate-policy.md) | **A non-degenerate numerator per table** for the family being tolerated: at least six monthly observations per table with a reject count ≥ 10, so the Poisson relative error falls under ~30%. empresas (numerator 1) and estabelecimentos (numerator 0 for that family) satisfy neither today, and more months of "1" and "0" will not change that. What would change it is the source getting dirtier — which is the event the gate exists to catch, so the condition is self-consistent rather than circular. | **UNCLOSABLE** | 2026-08-18 | ADR 0006's own words: *"NOT SHIPPED, AND NO CODE CAN SHIP IT. This condition is evidentiary"* — it asks for observations the source has to supply, and no change to this repository can supply them |
| [0006](0006-bronze-dq-gate-policy.md) | **The reclaim decoupling shipped first**, so that a threshold, if it is ever adopted, is adopted for what it does and not as a workaround for a wiring defect. | **MET** | 2026-08-18 | F4 Task 2 wired `reclaim_landing` onto the triage path; the 2026-08-18 run freed 8,212,278,423 B (`docs/f4-run-evidence.md`, ADR 0006 line 536) |
| [0010](0010-observation-ledger-over-a-lossy-extract.md) | **The derivation ceasing to be cheap.** "Derive, do not materialise" rests on 93 s / 26 s, both single first-run observations on today's data volumes. A phase that consults the ledger many times per job, or a key space that grows materially, should re-measure before inheriting the decision — and materialise on the number rather than on the inconvenience. | **NOT READ** | — | *nobody has taken this reading* |
| [0010](0010-observation-ledger-over-a-lossy-extract.md) | **The gate moving.** If the row-level DQ quarantine is ever replaced by file-level rejection or by soft downstream rules, the extract stops being lossy in the way this ADR models, `rejected_by_our_gate` becomes unreachable, and the five states collapse toward the literature's own patterns. That is Option 1, rejected here on cost rather than on principle. | **NOT READ** | — | *nobody has taken this reading* |
| [0010](0010-observation-ledger-over-a-lossy-extract.md) | **The quarantine losing its durability.** The ledger's whole distinction depends on the quarantine tables being retained per month as reliably as bronze. A retention or vacuum policy that broke that would not degrade this decision, it would falsify it — the ledger would keep answering, and would report our own rejections as departures. | **NOT MET** | 2026-08-30 | `src/opl/bronze/retention.py` reclaims landed inner CSVs from the Volume and nothing else; it reads the quarantine only to account rows. No policy in this repository deletes a quarantine row |
| [0010](0010-observation-ledger-over-a-lossy-extract.md) | **A published compression pattern for a record tracking satellite.** The search behind this ADR looked for one and found none, which is what makes 3a cost what it costs. A real one would reopen 3a on its merits. | **NOT READ** | — | *nobody has taken this reading* |
| [0010](0010-observation-ledger-over-a-lossy-extract.md) | **A source delivering deletes.** Everything here exists because absence has to be interpreted. A feed that states a departure removes the need to infer one, and the ledger would become a cross-check rather than the mechanism. | **LOOKS MET, IS NOT** | 2026-08-30 | F-DB produced **16 hard DELETEs** (`docs/f-db-run-evidence.md`) — but ADR 0017 Decision 2 lands FULL snapshots and DERIVES the diff in the lakehouse, so no source states a departure. Absence is still inferred, which is the whole premise this condition would remove |
| [0011](0011-no-hub-socio-and-a-disappearance-driven-effectivity-satellite.md) | **The RFB unmasking `cpf_cnpj_socio`, or publishing any stable partner identifier.** That is the only thing that makes a partner a business object this vault can key on, and it would make `hub_socio` correct rather than merely prescribed. Nothing short of it helps: adding the name does not, because the name reads `***`. | **NOT READ** | — | *nobody has taken this reading* |
| [0011](0011-no-hub-socio-and-a-disappearance-driven-effectivity-satellite.md) | **A grant admitting us to `opl_pii_readers` does NOT change this.** It unmasks `nome_socio_razao_social`, which is a UC mask over a real value; the CPF mask is the RFB's, in the file, and is irreversible for every reader. The two are separate and the ADR keeps them separate for exactly this reason. | **NOT READ** | — | *nobody has taken this reading* |
| [0011](0011-no-hub-socio-and-a-disappearance-driven-effectivity-satellite.md) | **A partner-count distribution that made a driving key meaningful.** §3a was rejected because 49.7% of companies with partners have more than one at once. On a relationship that really is one-to-one over time, §3a is the better pattern and this rejection does not carry over to it. | **NOT READ** | — | *nobody has taken this reading* |
| [0011](0011-no-hub-socio-and-a-disappearance-driven-effectivity-satellite.md) | **Departures caused by our own gate ceasing to be possible** — see ADR 0010's own version of this. If the quarantine stopped removing rows from bronze, the gate on `CLOSING_STATE` would still be correct but would no longer be load-bearing, and `closed_by` would record a distinction that cannot vary. | **NOT READ** | — | *nobody has taken this reading* |
| [0011](0011-no-hub-socio-and-a-disappearance-driven-effectivity-satellite.md) | **Measuring how many `rejected_by_our_gate` keys have an OPEN window.** This is the number nobody has, and it is what sizes the consequence of the gate: 1,781 keys per month are protected by it, but how many of those would have had a window closed is unmeasured. A large answer strengthens the gate; a zero answer would mean the protection is real but never yet exercised, which is worth knowing before anyone simplifies it away. | **NOT MET** | 2026-08-30 | still the number nobody has. `rejected_by_our_gate` is the one state of ADR 0010's five-state model that has never had a witness in any source (F7 plan §0.4), so there is nothing to measure the open windows over |
| [0012](0012-two-spellings-of-the-business-key-hash.md) | Spark exposing an upper-casing whose Unicode version can be pinned independently of the JDK's. That removes the second half of the problem and leaves only the ordinary second-spelling risk. | **NOT READ** | — | *nobody has taken this reading* |
| [0012](0012-two-spellings-of-the-business-key-hash.md) | A feed that must carry one of the forty as part of a business key. That makes the divergence reachable rather than latent, and forces a choice between pinning the runtime and normalising before hashing. | **LOOKS MET, IS NOT** | 2026-08-30 | the UTF-8 feed arrived (F-DB merchant) and the project took a THIRD option neither branch of this condition names: reject at the bronze gate under `unhashable_case_divergence` (`src/opl/bronze/rules.py`). Merchant's keys are ASCII, so the divergence stays latent and the choice is still unforced |
| [0012](0012-two-spellings-of-the-business-key-hash.md) | A measurement showing the UDF cost is affordable on the target cluster. That would collapse the two spellings back into one, which is strictly better if it can be paid for. | **NOT READ** | — | *nobody has taken this reading* |
| [0013](0013-two-satellites-on-hub-estabelecimento.md) | A later month-pair bringing `_dados` and `_endereco` close together. The cut would then be legible but not economic. (The re-run that closed the original scope gap moved them **further apart**, 1.9× → 2.13×, not closer.) | **NOT READ** | — | *nobody has taken this reading* |
| [0013](0013-two-satellites-on-hub-estabelecimento.md) | A cross-tab showing `situacao_cadastral` and `motivo_situacao_cadastral` change on substantially disjoint rows. That breaks the argument bounding where a third cut could go. | **NOT READ** | — | *nobody has taken this reading* |
| [0013](0013-two-satellites-on-hub-estabelecimento.md) | A downstream consumer of `nome_fantasia` alone, at a volume that makes the 30× ride-along a real cost rather than a stated one. | **NOT READ** | — | *nobody has taken this reading* |
| [0014](0014-dim-company-at-empresa-grain.md) | Payments whose counterparties are genuinely 14 characters — a real feed, or the F1b re-run of option 3. Then `dim_merchant` is buildable and this ADR is superseded rather than amended, because the star's grain changes. | **NOT READ** | — | *nobody has taken this reading* |
| [0014](0014-dim-company-at-empresa-grain.md) | A satellite on `link_empresa_estabelecimento` carrying `identificador_matriz_filial`. That alone unblocks a company-grained `dim_geography` **without** touching the fact, which makes it the cheapest of the two paths and the one to take first if either is taken. | **NOT READ** | — | *nobody has taken this reading* |
| [0014](0014-dim-company-at-empresa-grain.md) | A customer entity arriving in F2 wave 2. `dim_customer` then becomes a deferral rather than out of scope, and this table gets a fifth row. | **NOT MET** | 2026-08-30 | F2 wave 2 is unstarted, and F7's plan §8 names it as such rather than deferring it |
| [0015](0015-as-of-known-time-and-append-only-scd2.md) | An RFB feed carrying a business effective date at empresa grain. The dimension becomes bi-temporal and this ADR is superseded, not amended. | **NOT READ** | — | *nobody has taken this reading* |
| [0015](0015-as-of-known-time-and-append-only-scd2.md) | A snapshot cadence fine enough that the known/valid gap stops mattering. It would not change the model, only the size of the caveat. | **NOT READ** | — | *nobody has taken this reading* |
| [0015](0015-as-of-known-time-and-append-only-scd2.md) | A dimension large enough that drop-and-rebuild stops being affordable. That is the argument for a `MERGE`, and it is a real one at a scale this project has not reached: `dim_company` — **69,202,818 rows**, one per satellite version plus the ghost — **built in 120 s** on Free Edition serverless, against an extrapolation of 2,000–6,000 s. **The extrapolation was falsified and the measurement is what this bullet rests on**; a rebuild costs two minutes, so the refusal above is cheap to comply with and `MERGE` buys nothing yet. | **NOT MET** | 2026-08-13 | the opposite of met, and the ADR was amended in place to say so: `dim_company`'s 69,202,818 rows **built in 120 s** on Free Edition serverless, against this bullet's own 2,000-6,000 s extrapolation, which the ADR records as falsified. A rebuild costs two minutes |
| [0015](0015-as-of-known-time-and-append-only-scd2.md) | Tests that no longer read timestamps back into Python on a platform with this limit. 9999-12-31 would then be available, and would be worth taking only for conventional familiarity. | **NOT READ** | — | *nobody has taken this reading* |
| [0018 D1](0018-dataops-derives-it-does-not-instrument-and-it-does-not-act.md) | the moment anything **branches** on `collapsed_duplicates` rather than printing it. The write then belongs in that change, keyed on the task run id, in the loaders that produce the number. | **NOT READ** | — | *nobody has taken this reading* |
| [0019 D1](0019-the-proof-runs-where-a-process-can-be-killed.md) | a measurement, on the compute of the day, that a Kafka client cannot reach an external broker. The claim is falsifiable now in a way it was not when it was written, and that is the whole of what changed. | **NOT READ** | — | *nobody has taken this reading* |
| [0019 D2](0019-the-proof-runs-where-a-process-can-be-killed.md) | a way to terminate a task's process at a chosen instant on the deploy target. Until that exists, moving this proof onto the platform costs it its falsifier, and a proof without a falsifier is a demo. The evidence document, the job's YAML header and the job's own stdout each carry that refusal, because it is the misreading this phase is most likely to attract. | **NOT READ** | — | *nobody has taken this reading* |
| [0019 D3](0019-the-proof-runs-where-a-process-can-be-killed.md) | a transport whose payload cannot be the file path's bytes — Avro or Protobuf behind a schema registry. Then the second serialiser is the deliverable rather than the defect, and the digest moves inside it rather than being asserted across it. | **NOT READ** | — | *nobody has taken this reading* |
| [0019 D4](0019-the-proof-runs-where-a-process-can-be-killed.md) | a source with no per-delivery coordinate. The identity would then have to be carried in the payload, and it is the instrument that would need rebuilding, not the guarantee. | **NOT READ** | — | *nobody has taken this reading* |
| [0019 D5](0019-the-proof-runs-where-a-process-can-be-killed.md) | a contract in which a repeated business tuple is itself a defect. The key would move, and the 800 would become something to catch rather than something to protect. | **NOT READ** | — | *nobody has taken this reading* |
| [0019 D6](0019-the-proof-runs-where-a-process-can-be-killed.md) | a broker with a lifetime longer than the reader's — a paid or self-hosted cluster this repository can point at. The table then joins the registry, and it joins it **with a declared cadence**, because ADR 0018's second decision refuses a freshness metric without one. | **NOT READ** | — | *nobody has taken this reading* |
| [0019 D7](0019-the-proof-runs-where-a-process-can-be-killed.md) | a deployment where the bootstrap host or the username is itself sensitive — a shared broker whose tenant is inferable from either. The bootstrap **is** a secret here for that reason, and the cost of that is visible rather than hidden: a `[REDACTED]` elsewhere in the task log is that scrubbing working as intended, and the evidence document says so before quoting the log. | **NOT READ** | — | *nobody has taken this reading* |
| [0019 D8](0019-the-proof-runs-where-a-process-can-be-killed.md) | a compute that hands the cap over. The cap arm is unchanged and takes precedence wherever it can run; nothing here loosens it. | **NOT READ** | — | *nobody has taken this reading* |
| [0020 D1](0020-the-triage-accelerator-is-deterministic-and-the-model-runs-as-the-control.md) | an input class that is genuinely unstructured — free-text operator notes, vendor emails, a stack-trace corpus. The classifier would then have a job no `CASE` expression can do, and the model would earn a place **behind** the deterministic verdict rather than instead of it. | **NOT READ** | — | *nobody has taken this reading* |
| [0020 D2](0020-the-triage-accelerator-is-deterministic-and-the-model-runs-as-the-control.md) | nothing reverses running a control. What would change its *conclusion* is the control outperforming the ladder on the same corpus — which would be a real result and would be published as one. | **NOT READ** | — | *nobody has taken this reading* |
| [0020 D3](0020-the-triage-accelerator-is-deterministic-and-the-model-runs-as-the-control.md) | a consumer that needs history *of triage* rather than history of incidents — a trend over what was recommended and whether it was followed. That is a different product, and it would need the retry question answered first. | **NOT READ** | — | *nobody has taken this reading* |
| [0020 D4](0020-the-triage-accelerator-is-deterministic-and-the-model-runs-as-the-control.md) | a run that settles prediction 3, plus a reason to want the issue opened by something other than a person. | **NOT READ** | — | *nobody has taken this reading* |
| [0020 D5](0020-the-triage-accelerator-is-deterministic-and-the-model-runs-as-the-control.md) | Unity Catalog lineage becoming readable and complete on this edition, at which point the declaration becomes the *lock* on a derived answer rather than the answer. | **LOOKS MET, IS NOT** | 2026-08-29 | lineage IS readable — `system.access.table_lineage`, **3,327 rows, 72 distinct target tables**, 2026-07-24 to 2026-08-28, statement `01f1a4c2-1cfb-115a-a947-5a2fbc1aec10` — and it carries the hard `bronze_payments -> fact_payment` edge this decision said a vault-path walk would miss. It is not COMPLETE: it records EXECUTIONS (`event_time`) while the manifest states STRUCTURE, so it answers *nothing downstream* for a table whose loader has not run inside the retention window |
| [0020 D6](0020-the-triage-accelerator-is-deterministic-and-the-model-runs-as-the-control.md) | the telemetry marking a retired task key as superseded, which would make the stable-key argument unnecessary rather than wrong. | **NOT READ** | — | *nobody has taken this reading* |

## What this page cannot see

**The reversal-condition extractor reads three spellings and nothing else.** Which ADRs
use each is read out of the files, not typed here:

- a `### What would reverse this decision` section — 0006
- a `### What would change this decision` section — 0010, 0011, 0012, 0013, 0014, 0015
- an inline `**What reverses it:**` paragraph under a numbered decision — 0018, 0019, 0020

**And the grammar underneath them, which is the floor a condition has to clear to be
counted at all.** Code fences and the HTML that renders as nothing — comments, and
`<pre>`, `<script>`, `<style>` and `<textarea>` blocks — are masked out before either
reading parses anything, so a heading or a `**What reverses it:**` line inside one neither
ends a section early nor invents a condition. Inside a conditions section a bullet is `-`,
`*`, `+` or `N.`, indented at most three spaces; one indented four or more is not counted
as a condition of its own. Every reversal section in a file is read, not only the first.
Each condition is flattened to a single line and cut at the first blank line, so the
argument under it stays in the ADR, which is the better document for it.

**The two readings SHARE that mask, and therefore share its blind spots.** Everything else
about them differs — this generator captures with multi-line regexes, the test walks lines
— but the mask is one algorithm typed twice, so neither reading can catch the other being
wrong about a fence or an HTML block. It is typed twice rather than imported for one
reason: `tests/test_adr_index.py` has to go on reading the ADR files when
`scripts/adr_index.py` REFUSES to import, which is what it does the moment an ADR arrives
with no declared phase. A test asserts the two masks agree over a battery of constructs,
so a one-sided edit to either goes red.

Two things this leaves uncounted, and neither is hidden:

- **ADR 0013 carries a fourth spelling** in its Consequences — *"A second month-pair
  could reverse this"* — which is not counted here.
- **ADR 0018's Consequences claim four refusals "each with a reversal condition"** while
  only its Decision 1 uses the marker.

**A count whose extractor's limit is not written down is a count nobody can check.**
Widen the extractor in `scripts/adr_index.py` if a fourth spelling is worth adopting;
do not widen it by editing this file.
