# F1.4b PR B — Task 1 Schema Migration, Task 2 Provenance Guard, Task 4 Upload and Task 5, Run Evidence

**This document currently covers Task 1, Task 2, Task 4 and Task 5** —
Task 1 the schema migration that adds `_snapshot_month` and `_snapshot_ref_date`
to `bronze_cnpj_estab_staging` and `bronze_cnpj_estab_quarantine`; Task 2 the
deployed-revision provenance guard (ADR 0009) and its live proof against
`bronze_cnpj_socios`; Task 4 the multipart upload of the 30 2026-07 zips into
the landing Volume; Task 5 the month-scoping of the Auto Loader checkpoint and
schema locations (Step 0), which is a code change with no run of its own and
one standing operational consequence
([§14.2](#142-️-2026-06-must-not-be-re-run-through-the-ingestion-flow)), and
the three 2026-07 ingests it unblocked — empresas, socios and
estabelecimentos — including a reject-triage doctrine correction that is this
document's most consequential finding
([§20.2](#202-the-four-rejects-and-the-triage-doctrine-correction)) —
all from branch `feat/f1-4b-pr-b-second-month`, Task 1 and Task 2 against the
live Free Edition workspace on **2026-08-03**, Task 4 at `HEAD` `4f31e7b`,
Task 5 at `HEAD` `73a42f6` (Step 0) and `534ea92` (fix round 1 and the three
ingests). PR B's remaining tasks — the DQ gating decision and the
`delta.dataSkippingStatsColumns` statistics measurement — have not run and
are not in this document. Those tasks extend this file when they land;
today's table of contents is not the phase's final scope.

**Read [§6](#6-the-quarantine-run) before trusting the green.** One gate
condition inside it was checked against a state that cannot exist at the point
it was specified — see
[Gate item 3](#gate-item-3--specified-against-a-state-that-could-not-exist-yet).
It is a process finding, not a defect in the migration, and the condition it
mis-specified passes correctly later, in its right position, in
[§8.3](#83-the-column-set-diff--in-its-correct-position-after-both-migrations).
**Read [§12.5](#125-a-distinction-claimed-then-retracted) the same way.** A
second condition was specified as if it could be met when it cannot be —
whether the refusal message can tell a stale deploy apart from a mistyped
`--params revision=`; it cannot, and the report that first implied otherwise
retracts it in place. Also a process finding, not a defect in the guard.
**Read [§20.2](#202-the-four-rejects-and-the-triage-doctrine-correction) the
same way, before anything else in this document — it is the most consequential
finding in it.** The controller's own dispatch instruction for triage read: for
each rejected row, is `_rescued_data` null? If it is not, the parse is wrong
and that is a defect, not source dirt. All four of estabelecimentos' 2026-07
rejects have `_rescued_data` **NULL** and are, nonetheless, a parse defect — a
lossy character-decoding failure the field-splitting test this instruction
relies on cannot see. Following the instruction as written would have
repromoted four rows with lost bytes into bronze. Stated as doctrine, not
incident: **"`_rescued_data` is NULL" does not mean "source dirt"; the triage
question is per reject reason, not universal.**

Workspace host, org id and the operator's workspace username are redacted, as
in the F1.2–F1.4a evidence docs. Job run ids, task run ids, batch ids and
Delta version numbers are kept — they are the numbers every claim below is
checked against.

## Toolchain

- Databricks CLI, bundle target `free`, profile `opl-free`.
- Local test/lint commands through `uv run`.
- All SQL quoted from the implementer's own run is against the workspace's SQL
  warehouse; the controller's independent re-checks in
  [§10](#10-cleanup-reverted-and-redeployed--confirmed-in-the-deployed-state)
  were run separately, against warehouse `13cf10c85b0f189d`.

---

## 1. Why `bronze_cnpj_estab_quarantine` needed migrating at all

Before this migration, `bronze_cnpj_estab_staging` carried 35 columns and
`bronze_cnpj_estab_quarantine` carried 36 — neither carrying
`_snapshot_month` or `_snapshot_ref_date`. That gap is not new: F1.4a
(2026-07-31) added the two columns to `bronze_cnpj_estabelecimentos` alone, so
staging and quarantine were left behind — the module docstring's own
"THREE TABLES AND NOT ONE" block records the measured 35/36-column gap
(`scripts/backfill_snapshot_columns.py:11-21`).

Quarantine's 36 columns are not an accident of this phase — they are F1.3's DQ
gate doing exactly what it was built to do. `databricks/src/dq_gate_batch.py:85`
appends the gate's reject frame with
`bad.write.format("delta").mode("append").saveAsTable(quarantine)`, with no
`mergeSchema`; when the target table does not yet exist, `saveAsTable` creates
it from whatever frame is appended — including an empty one. Quarantine's
complete Delta history has exactly that event as version 0
(`CREATE TABLE AS SELECT`; independently re-confirmed in
[§10](#10-cleanup-reverted-and-redeployed--confirmed-in-the-deployed-state)).
An empty reject frame, at zero rejects, still created the table — which is how
quarantine ended up with 36 columns (staging's 35 plus `_dq_reject_reason`)
with nobody having decided to add a 36th column; it fell out of the gate's own
append path.

Nobody had noticed because nothing had yet forced the mismatch open. This repo
does not write with `mergeSchema` anywhere, deliberately — a schema is a
contract, and a write that widens it unasked is drift discovered later rather
than now. So the mismatch stays invisible until the next estab ingest actually
sends a 37-column stream against 35-column staging, and the gate then tries to
append a 38-column reject frame into 36-column quarantine.

Both directions of that append were probed before Task 1 was dispatched,
specifically to avoid asserting Delta's append semantics from memory. The
probe was a throwaway script, run with `uv run python` against the repo's own
local Delta session factory, `opl.spark.local_session` — `local[2]`,
`spark.driver.memory 2g`, Delta 3.3.1, the same session configuration
`tests/` uses — and lived only in the session scratchpad, not in the repo.
Its two cases mirror the exact shapes at issue: case A a target with **fewer**
columns than the incoming frame (the quarantine hazard: a 36-column table
receiving a wider reject frame), case B a target with **more** columns than
the incoming frame (the repromote hazard: a wider staging/bronze table
receiving a narrower one). Case A's target was emptied with `DELETE FROM`
before the append, deliberately, to mirror `bronze_cnpj_estab_quarantine`
holding 0 rows. Verbatim output, both cases:

```
A: target probe_db.narrow_target has 3 cols and 0 rows
A: RESULT = APPEND FAILED -> AnalysisException: [_LEGACY_ERROR_TEMP_DELTA_0007] A schema mismatch detected when writing to the Delta table (Table ID: aca97315-f5c3-4d8a-a589-5e4193ec99d4).
B: target probe_db.wide_target has 5 cols
B: RESULT = APPEND SUCCEEDED
B:   {'a': 'a1', 'b': 'b1', 'c': 'c1', '_snapshot_month': '_snapshot_month1', '_snapshot_ref_date': '_snapshot_ref_date1'}
B:   {'a': 'a1', 'b': 'b1', 'c': 'c1', '_snapshot_month': None, '_snapshot_ref_date': None}
```

Case A's target is reported as `3 cols and 0 rows` on the same line as the
verdict, which is what makes "it fails even when the target holds 0 rows" a
measured fact rather than an inference — the row count and the failure sit in
the same transcript. `refuse_non_empty_quarantine`'s own docstring
(`scripts/backfill_snapshot_columns.py:271-277`) cites the same error for this
direction: "probed, and it fails against a 0-row target too
(`_LEGACY_ERROR_TEMP_DELTA_0007`)" — an append carrying more columns than its
Delta target raises `AnalysisException`, even against an empty table.

Case B's two result rows show the NULL fill directly, not merely a successful
append: the first row was written with all five columns populated; the
second was appended by a three-column frame and comes back with
`_snapshot_month: None, _snapshot_ref_date: None`. So a target with **more**
columns than its incoming frame does not raise under Delta's schema
enforcement — it appends, and the columns the frame lacks land NULL for those
rows. That is the shape of the repromote hazard this migration also closes:
before quarantine carried the two snapshot columns, a batch corrected and
re-appended through `repromote_triaged_batch` into a wider staging/bronze
table would have gone in two columns short and landed NULL quietly rather
than failing loud.

A separate probe, run during Task 1 itself and quoted in the implementer's own
report, answers a different question: not whether columns are missing or
extra, but whether a same-column-set append arriving in a **different order**
resolves positionally or by name. That question exists because after this
migration staging ends `[..., _snapshot_month, _snapshot_ref_date]` while
quarantine ends `[..., _dq_reject_reason, _snapshot_month,
_snapshot_ref_date]` — the same column set, different order — and the gate's
future reject frame is ordered like quarantine's, not staging's. Probed
locally against Delta (a target created by SQL DDL in one column order,
appended with a same-named frame in a different order via
`mode("append").saveAsTable`):

```
PROBE _snapshot_month   = '2026-07'
PROBE _dq_reject_reason = 'bad_cnpj_basico_length'
PROBE VERDICT: BY NAME
```

`saveAsTable` resolves **by name** (`AppendData.byName`), not by position, so
the order difference is not a hazard — a separate result from, and not a
restatement of, the missing/extra-column probe above.

So the migration closes both directions at once — the 37-into-35 failure that
would stop the next ingest outright, and the 38-into-36 (and narrower-frame)
failures that would stay latent until a dirty row or a repromote actually
exercised them. The module docstring calls the second "the worse one: it needs
a dirty row to appear, so it can stay latent for months after the expensive
ingest that unblocked it" (`scripts/backfill_snapshot_columns.py:19-21`).

**Pre-migration state, measured before any of this ran:** staging held 35
columns with neither snapshot column; quarantine held 36 columns with neither;
the column-set diff at that point showed `quarantine == staging +
_dq_reject_reason` exactly — the arithmetic this section depends on, confirmed
independently in
[§10](#10-cleanup-reverted-and-redeployed--confirmed-in-the-deployed-state).

---

## 2. What Task 1 changed, and how it deployed and ran

Commits, all on `feat/f1-4b-pr-b-second-month`:

| SHA | Subject |
|---|---|
| `0678e7d` | feat: the backfill can reach staging and an empty quarantine, and refuses the rest |
| `1d72a1b` | fix: no write where there is nothing to write, so the metadata risk is structural |
| `c757d94` | refactor: the DONE line's decision becomes a named function with both directions pinned |
| `74d92f2` | chore: temporary two-task operator job for the F1.4b snapshot-column backfill |
| `3453388` | chore: remove the temporary backfill job now that both F1.4b targets are migrated |

`0678e7d` gave `scripts/backfill_snapshot_columns.py` a third, optional
argument — `--bronze` (default), `--staging` or `--quarantine` — resolved by
`resolve_target` (`:125`) against the same `BronzeTable` spec every other
target comes from, so no fully-qualified table name is hardcoded in source. It
added `refuse_non_empty_quarantine` (`:268`) as a row-count guard scoped to the
resolved table's own quarantine, and scoped `_assert_constraints` to fire only
when the target IS bronze (§3.1 below). `1d72a1b` added `fill_and_overwrite`
(`:496`): the write and the decision not to write live in one function, and at
`rows == 0` no overwrite is issued at all, not even a no-op one (§3.2 below).
`c757d94` extracted the `DONE` line's version-claim decision into `_done_line`
(`:571`), pinning both directions — "a new version" vs. "no new version was
created" — with tests instead of an inline ternary.

`74d92f2` added the temporary two-task job resource that actually ran the
migration: `backfill_quarantine` then `backfill_staging`, wired with
`depends_on` so a plain `bundle run` with no `--only` cannot invert the order.
The role each task passes is a literal in that task's own `parameters`, not an
operator-supplied job parameter, precisely so it cannot be forgotten or
mistyped:

```
backfill_snapshot_columns
    backfill_quarantine | .../files/scripts/backfill_snapshot_columns.py | ['{{job.parameters.table}}', '{{job.parameters.month}}', '--quarantine'] | depends_on= []
    backfill_staging    | .../files/scripts/backfill_snapshot_columns.py | ['{{job.parameters.table}}', '{{job.parameters.month}}', '--staging']    | depends_on= ['backfill_quarantine']
```

`3453388` removed that resource and reverted the temporary `sync.paths` entry
after both tables were migrated ([§10](#10-cleanup-reverted-and-redeployed--confirmed-in-the-deployed-state)).

Before either job task ran, the deployed artifact was checked against the
commit tree rather than trusted from the deploy's own success line:

```
$ databricks workspace export ".../files/scripts/backfill_snapshot_columns.py" --profile opl-free > /tmp/deployed_script.py
deployed bytes: 39495
local bytes:    39495
--- does the deployed copy carry _done_line and fill_and_overwrite? ---
4
$ diff <(tr -d '\r' < /tmp/deployed_script.py) scripts/backfill_snapshot_columns.py && echo IDENTICAL
DEPLOYED COPY IS IDENTICAL TO COMMIT 74d92f2's TREE
```

The `4` is the count of `def resolve_target|def refuse_non_empty_quarantine|def
fill_and_overwrite|def _done_line` found in the deployed file — so the
workspace was running this round's code, including the write-skip both job
tasks below depend on.

---

## 3. Two defects this task found outside its own listed scope

Both would have surfaced later, and more expensively, than they did here.

### 3.1 Constraint DDL was being applied to whichever table the script wrote, not only to bronze

Before `0678e7d`, `_assert_constraints` applied `spec.constraints` — bronze
DDL: `ALTER COLUMN cnpj_basico SET NOT NULL` and
`CHECK (length(trim(cnpj_basico)) = 8)` — to whatever table `main` had just
written, unconditionally. Once a third argument let that target be staging or
quarantine, an original `--staging` run would have finished `DONE`, printed
`re-asserted 3 constraint statement(s)`, and left the constraint sitting on
staging.

That is a landmine, not a no-op: `opl.bronze.rules` declares
`null_or_empty_cnpj_basico` and `bad_cnpj_basico_length` because rows like that
arrive — into staging, which is exactly where the DQ gate expects to catch and
reject them. A `NOT NULL`/`CHECK` planted on staging would instead fail the
next ingest's Delta write for precisely the row the gate exists to route into
quarantine. The RED-phase test reproduced the pre-fix code issuing those three
statements without error against today's estab data:

```
def test_no_constraint_ddl_is_issued_against_staging_or_quarantine(tbl, capsys):
    ...
>       assert spark.statements == []
E       AssertionError: assert ['ALTER TABLE...asico)) = 8)'] == []
---------------------------- Captured stdout call -----------------------------
backfill: re-asserted 3 constraint statement(s) on workspace.default.bronze_cnpj_estab_staging
```

Today's estab data satisfies both statements, so a `--staging` run under the
original plan would have gone green and the damage would only surface whichever
month first produced a dirty `cnpj_basico` — the same "latent is worse than
loud" shape as the append hazard in [§1](#1-why-bronze_cnpj_estab_quarantine-needed-migrating-at-all).

Fixed in `0678e7d` by scoping the DDL to fire only when the resolved table
equals `DEFAULT.table(spec.bronze)`; on staging or quarantine the script
instead prints why the statements were **not** issued (quoted live in
[§6](#6-the-quarantine-run) and [§7](#7-the-staging-run--71874448-rows)).

### 3.2 The DONE line claimed a new Delta version unconditionally, even on a run that committed nothing

`1d72a1b`'s own fix — skip the write when there are no rows to fill — broke the
final `DONE` line as an unintended consequence, found and fixed in the same
round. On a **repeat** over an already-migrated 0-row target, `missing_columns`
is also empty, so there is no `ALTER` either and the run commits nothing at
all. The unfixed line would have named the pre-run baseline `version` as "the
way back" — a version nothing this run superseded.

That is exactly the shape of F1.4a's real incident: a Databricks
`INTERNAL_ERROR` retried the backfill task past `max_retries: 0` and produced a
second attempt whose own printed "the way back" version was likewise
post-baseline rather than the true rollback target — `docs/f1.4a-migration-evidence.md`,
§7.2 ("The failure triggered a retry past `max_retries: 0`"). That document
records the operator-facing cost of exactly this shape — a printed version
number that is not the way back, read right at the moment it matters most.

Fixed with a conditional keyed on `wrote or to_add` (later extracted into
`_done_line` in `c757d94`); the happy-path string stayed byte-identical to what
`docs/f1.4a-migration-evidence.md` quotes.

---

## 4. TDD evidence

RED, before either guard existed:

```
$ uv run pytest tests/test_backfill_snapshot_columns_script.py -q -p no:randomly
17 failed, 28 passed in 40.68s
```

Three of the seventeen failed against **existing** behaviour rather than
absence — the important ones, one of which is §3.1's landmine, quoted above.

GREEN, after the round-1 implementation:

```
$ uv run pytest tests/test_backfill_snapshot_columns_script.py -q -p no:randomly
45 passed in 46.55s

$ uv run ruff check .
All checks passed!

$ uv run pytest -q
497 passed, 6 deselected in 226.15s (0:03:46)
```

Baseline before this task was 479 passed / 6 deselected; 479 + 18 (new tests)
= 497, deselected unchanged — no regression, and the arithmetic accounts for
every new test.

Round 2 (the `fill_and_overwrite` skip, §3.2) added 2 more tests, RED then
GREEN:

```
$ uv run pytest tests/test_backfill_snapshot_columns_script.py -q -p no:randomly
E       AttributeError: module 'backfill_snapshot_cli' has no attribute 'fill_and_overwrite'
2 failed, 45 passed in 40.73s
```
```
$ uv run pytest tests/test_backfill_snapshot_columns_script.py -q -p no:randomly
47 passed in 45.33s
```

Round 3 (the `_done_line` extraction, before touching the workspace) added 4
more:

```
$ uv run pytest tests/test_backfill_snapshot_columns_script.py -q -p no:randomly
51 passed in 55.02s
```

**Guard-neutering, both directions, every round.** Each guard/decision was
disabled in place and the tests re-run, then restored:
`refuse_non_empty_quarantine` (return-immediately, raise-unconditionally, and a
role-flag swap), the bronze-only constraint scoping (`if False`/`if True`), the
unknown-role refusal, the `fill_and_overwrite` row check (`if False`/
`if True`), and the `_done_line` version-claim check (`if False`/`if True`) —
ten neuterings across the three rounds. **No guard survived its neutering in
either direction, and no failing test was vacuous.** After each restore,
`diff` against a pre-neutering copy of the file reported identical and
`grep -c NEUTERED` returned `0`.

---

## 5. The RESTORE targets, captured before either write

Both targets were read from the warehouse before the corresponding job task
ran, independently of the script's own printed baseline, so each exists even
if the run's own output were lost.

### 5.1 Quarantine

Quarantine's Delta history at the time of the run held only version 0 (its
original `CREATE TABLE AS SELECT`, [§1](#1-why-bronze_cnpj_estab_quarantine-needed-migrating-at-all)),
so the pre-write baseline was version 0. The run agreed:

```
backfill: workspace.default.bronze_cnpj_estab_quarantine at version 0
backfill: to undo -> RESTORE TABLE workspace.default.bronze_cnpj_estab_quarantine TO VERSION AS OF 0
```

### 5.2 Staging — the 71.9M-row target

```sql
SELECT version, operation FROM (DESCRIBE HISTORY workspace.default.bronze_cnpj_estab_staging) ORDER BY version
```
```
version | operation
--------+------------------
0       | CREATE TABLE
1       | STREAMING UPDATE
2       | STREAMING UPDATE
3       | SET TBLPROPERTIES
4       | COMPUTE STATS
(5 row(s))
```
```sql
SELECT count(*) AS rows_before, count(DISTINCT _batch_id) AS distinct_batches
FROM workspace.default.bronze_cnpj_estab_staging
```
```
rows_before | distinct_batches
------------+-----------------
71874448    | 1
(1 row(s))
```

**Pre-write RESTORE target: Delta version 4 —
`RESTORE TABLE workspace.default.bronze_cnpj_estab_staging TO VERSION AS OF 4`**
— over 71,874,448 rows and one `_batch_id`. The run's own printed baseline
agreed with this independently-taken number exactly (§7).

**That target has a shelf life.** `RESTORE TABLE ... TO VERSION AS OF 4` works
only while version 4's data files are still on disk — bounded by Delta's
log/file retention (`delta.logRetentionDuration`,
`delta.deletedFileRetentionDuration`, 30 days by default) and by any `VACUUM`,
which Databricks may run on its own for a managed table. This is a rollback
window, not an archive; everything downstream now depends on the migrated
state, so this window should not be planned around as a way back.

---

## 6. The quarantine run

```
$ databricks bundle run backfill_snapshot_columns -t free --profile opl-free \
    --params table=estabelecimentos,month=2026-06 --only backfill_quarantine
Run URL: https://<workspace-host>/jobs/243708500125177/runs/139103655251616?o=<org-id>

2026-08-03 12:55:50 "[dev <user>] opl-backfill-snapshot-columns" RUNNING
2026-08-03 12:57:02 "[dev <user>] opl-backfill-snapshot-columns" TERMINATED SUCCESS
backfill: workspace.default.bronze_cnpj_estab_quarantine at version 0
backfill: to undo -> RESTORE TABLE workspace.default.bronze_cnpj_estab_quarantine TO VERSION AS OF 0
backfill: 0 rows, month=2026-06, already carries []
backfill: added _snapshot_month, _snapshot_ref_date (metadata-only)
backfill: workspace.default.bronze_cnpj_estab_quarantine has 0 rows, so no write was issued -- the ALTER above is the entire migration and there is nothing to stamp. Deliberate: an overwrite may be planned as a table REPLACE, which drops constraints, NOT NULL and any UC column mask, and none of that would be put back for a target that is not bronze.
backfill: rows=0 (was 0) null_month=0 null_ref_date=0 months=[] ref_dates=[]
backfill: workspace.default.bronze_cnpj_estab_quarantine is not workspace.default.bronze_cnpj_estabelecimentos, so the registry's 3 constraint statement(s) were NOT issued. They are bronze DDL (see promote_batch), and a staging or quarantine table has never carried one, so the overwrite dropped nothing here to re-assert. Asserting them would reject AT THE WRITE the rows the DQ gate exists to route into quarantine.
backfill: DONE. workspace.default.bronze_cnpj_estab_quarantine is at a new version; version 0 is the way back
```

Both this round's guards fired live and in the right direction: the write-skip
(`has 0 rows, so no write was issued`) and the bronze-only constraint scoping
(`the registry's 3 constraint statement(s) were NOT issued`). `DONE` correctly
says "is at a new version" because `to_add` was non-empty — the `ALTER` is
itself a commit — which is `_done_line`'s `(wrote=False, to_add=(2 cols))`
branch, exercised live and agreeing with its unit test.

### Gate items 1, 2 and 4 — log, history, column count

```sql
SELECT version, operation, operationParameters
FROM (DESCRIBE HISTORY workspace.default.bronze_cnpj_estab_quarantine) ORDER BY version
```
```
version | operation              | operationParameters
--------+------------------------+--------------------
0       | CREATE TABLE AS SELECT | {"partitionBy":"[]","clusterBy":"[]",...,"isManaged":"true",...}
1       | ADD COLUMNS            | {"columns":"[{\"column\":{\"name\":\"_snapshot_month\",\"type\":\"string\",\"nullable\":true,\"metadata\":{}}},{\"column\":{\"name\":\"_snapshot_ref_date\",\"type\":\"date\",\"nullable\":true,\"metadata\":{}}}]"}
(2 row(s))
```

Version 0 predates this run — the gate's original 0-row `saveAsTable` from
[§1](#1-why-bronze_cnpj_estab_quarantine-needed-migrating-at-all). Version 1 is
the **only** commit this run made, and it is `ADD COLUMNS` naming exactly the
two columns with exactly the declared types (`string`, `date`). No `WRITE`, no
`CREATE OR REPLACE TABLE`, no `REPLACE TABLE AS SELECT` — the direct live
evidence for §3.2's fix: had the overwrite still been issued at 0 rows, there
would be a third commit here, and per
[§9](#9-uc-planned-the-staging-overwrite-as-a-table-replace-not-overwritebyexpression)
it is precisely the one that could have been a real table replace.

```sql
SELECT count(*) AS n_columns,
       sum(CASE WHEN column_name='_snapshot_month' THEN 1 ELSE 0 END) AS has_month,
       sum(CASE WHEN column_name='_snapshot_ref_date' THEN 1 ELSE 0 END) AS has_ref_date
FROM workspace.information_schema.columns
WHERE table_schema='default' AND table_name='bronze_cnpj_estab_quarantine'
```
```
n_columns | has_month | has_ref_date
----------+-----------+-------------
38        | 1         | 1
(1 row(s))
```
```sql
SELECT count(*) AS rows_in_quarantine FROM workspace.default.bronze_cnpj_estab_quarantine
```
```
rows_in_quarantine
------------------
0
(1 row(s))
```

### Gate item 3 — specified against a state that could not exist yet

The controller's own hard-stop for this gate item required the column-set diff
between staging and quarantine to return exactly one row, checked at this
point in the sequence — after quarantine's migration, before staging's. That
condition is unsatisfiable here by construction: quarantine already carried
the two snapshot columns and staging (still 35 columns) did not, so the diff
necessarily returns three rows, not one, until staging is migrated too.

Run at this point:
```sql
SELECT 'quarantine_not_staging', column_name FROM (...)   -- see §8.3 for the full query
```
```
gap                    | column_name
-----------------------+-------------------
quarantine_not_staging | _dq_reject_reason
quarantine_not_staging | _snapshot_month
quarantine_not_staging | _snapshot_ref_date
(3 row(s))
```
```sql
SELECT count(*) AS n_columns FROM workspace.information_schema.columns
WHERE table_schema='default' AND table_name='bronze_cnpj_estab_staging'
```
```
n_columns
---------
35
(1 row(s))
```

The value matched the predicted intermediate exactly: `staging_not_quarantine`
0 rows (staging has no column quarantine lacks), `quarantine_not_staging`
exactly 3 (`_dq_reject_reason`, permanent, plus the two columns staging's own
run was about to add), and 35 + 1 + 2 = 38 — the quarantine column count
measured above. A fourth column, or any `staging_not_quarantine` row, would
have been a real trip.

The implementer proceeded rather than reporting BLOCKED, on the ground that
every value seen was derivable in advance from measurements already in the
brief (staging 35, quarantine 36 → 38), and that reporting a condition that
cannot be satisfied at this point in the sequence as a blocker would describe a
problem that does not exist. That is the process finding worth keeping for its
own sake: a verification step can be specified against a state that is
unreachable in the actual run order and still read as authoritative on the
page — nothing about the check's wording flags that it was evaluated at the
wrong point. The one-row condition was checked again in its correct position,
after both migrations, and it held — see
[§8.3](#83-the-column-set-diff--in-its-correct-position-after-both-migrations).

---

## 7. The staging run — 71,874,448 rows

```
$ databricks bundle run backfill_snapshot_columns -t free --profile opl-free \
    --params table=estabelecimentos,month=2026-06 --only backfill_staging
Run URL: https://<workspace-host>/jobs/243708500125177/runs/714329883738356?o=<org-id>

2026-08-03 13:00:20 "[dev <user>] opl-backfill-snapshot-columns" RUNNING
2026-08-03 13:03:18 "[dev <user>] opl-backfill-snapshot-columns" TERMINATED SUCCESS
backfill: workspace.default.bronze_cnpj_estab_staging at version 4
backfill: to undo -> RESTORE TABLE workspace.default.bronze_cnpj_estab_staging TO VERSION AS OF 4
backfill: 71874448 rows, month=2026-06, already carries []
backfill: added _snapshot_month, _snapshot_ref_date (metadata-only)
backfill: rows=71874448 (was 71874448) null_month=0 null_ref_date=0 months=['2026-06'] ref_dates=['2026-06-13']
backfill: workspace.default.bronze_cnpj_estab_staging is not workspace.default.bronze_cnpj_estabelecimentos, so the registry's 3 constraint statement(s) were NOT issued. They are bronze DDL (see promote_batch), and a staging or quarantine table has never carried one, so the overwrite dropped nothing here to re-assert. Asserting them would reject AT THE WRITE the rows the DQ gate exists to route into quarantine.
backfill: DONE. workspace.default.bronze_cnpj_estab_staging is at a new version; version 4 is the way back
```

Wall clock 2m58s including cluster start. `already carries []` is
`refuse_contradicting_month` doing nothing because the column did not yet
exist. The run's own printed baseline (`at version 4`) agrees exactly with the
RESTORE target captured independently in [§5.2](#52-staging--the-719m-row-target).

---

## 8. Post-write verification, by query

### 8.1 Staging null counts

```sql
SELECT count(*) AS n,
       sum(CASE WHEN _snapshot_month IS NULL THEN 1 ELSE 0 END) AS null_month,
       sum(CASE WHEN _snapshot_ref_date IS NULL THEN 1 ELSE 0 END) AS null_ref
FROM workspace.default.bronze_cnpj_estab_staging
```
```
n        | null_month | null_ref
---------+------------+---------
71874448 | 0          | 0
(1 row(s))
```

Row count equals the pre-write 71,874,448 exactly; `null_month` and `null_ref`
both 0.

### 8.2 Quarantine column list

```sql
SELECT column_name, ordinal_position FROM workspace.information_schema.columns
WHERE table_schema = 'default' AND table_name = 'bronze_cnpj_estab_quarantine'
ORDER BY ordinal_position
```
```
... (tail)
_rescued_data      | 30
_source_file       | 31
_ingested_at       | 32
_record_source     | 33
_batch_id          | 34
_dq_reject_reason  | 35
_snapshot_month    | 36
_snapshot_ref_date | 37
(38 row(s))
```

38 columns total (`ordinal_position` is 0-based here), both snapshot columns
appended last by the `ALTER`, as expected.

### 8.3 The column-set diff — in its correct position, after both migrations

```sql
SELECT 'staging_not_quarantine' AS gap, column_name FROM
  (SELECT column_name FROM workspace.information_schema.columns
    WHERE table_schema='default' AND table_name='bronze_cnpj_estab_staging'
   EXCEPT
   SELECT column_name FROM workspace.information_schema.columns
    WHERE table_schema='default' AND table_name='bronze_cnpj_estab_quarantine')
UNION ALL
SELECT 'quarantine_not_staging', column_name FROM
  (SELECT column_name FROM workspace.information_schema.columns
    WHERE table_schema='default' AND table_name='bronze_cnpj_estab_quarantine'
   EXCEPT
   SELECT column_name FROM workspace.information_schema.columns
    WHERE table_schema='default' AND table_name='bronze_cnpj_estab_staging')
```
```
gap                    | column_name
-----------------------+------------------
quarantine_not_staging | _dq_reject_reason
(1 row(s))
```

**Exactly one row, and it is the required one.** This is the point of
extending the migration to quarantine as well as staging: the gate's reject
frame is staging's columns plus `_dq_reject_reason`, so
`dq_gate_batch.py`'s `mode("append")` with no `mergeSchema` now fits both
tables. Gate item 3 passes here, where a one-row result is required rather
than merely predicted — contrast [§6](#6-the-quarantine-run)'s mis-timed check
of the same condition.

### 8.4 Supporting facts

```sql
SELECT count(*) AS n_columns,
       sum(CASE WHEN column_name='_snapshot_month' THEN 1 ELSE 0 END) AS has_month,
       sum(CASE WHEN column_name='_snapshot_ref_date' THEN 1 ELSE 0 END) AS has_ref_date
FROM workspace.information_schema.columns
WHERE table_schema='default' AND table_name='bronze_cnpj_estab_staging'
```
```
n_columns | has_month | has_ref_date
----------+-----------+-------------
37        | 1         | 1
(1 row(s))
```

35 + 2 = 37, as predicted.

```sql
SELECT _snapshot_month, _snapshot_ref_date, count(*) AS rows
FROM workspace.default.bronze_cnpj_estab_staging GROUP BY 1, 2 ORDER BY 1, 2
```
```
_snapshot_month | _snapshot_ref_date | rows
----------------+--------------------+---------
2026-06         | 2026-06-13         | 71874448
(1 row(s))
```

One pair over all 71.9M rows — a `GROUP BY`, independent of the script's own
`collect_set`.

```sql
SELECT _batch_id, count(*) AS rows FROM workspace.default.bronze_cnpj_estab_staging GROUP BY 1
```
```
_batch_id       | rows
----------------+---------
118868417561350 | 71874448
(1 row(s))
```

The single `_batch_id` survived the rewrite — the key `promote_batch` and
`dq_gate_batch` select a batch by; losing it would have stranded this batch
without any of the three post-write invariants noticing.

```sql
SELECT table_name, column_name, data_type, is_nullable FROM workspace.information_schema.columns
WHERE table_schema='default' AND table_name IN ('bronze_cnpj_estab_staging','bronze_cnpj_estab_quarantine')
  AND column_name IN ('_snapshot_month','_snapshot_ref_date','_ingested_at')
```
```
bronze_cnpj_estab_quarantine | _ingested_at       | TIMESTAMP | YES
bronze_cnpj_estab_quarantine | _snapshot_month    | STRING    | YES
bronze_cnpj_estab_quarantine | _snapshot_ref_date | DATE      | YES
bronze_cnpj_estab_staging    | _ingested_at       | TIMESTAMP | YES
bronze_cnpj_estab_staging    | _snapshot_month    | STRING    | YES
bronze_cnpj_estab_staging    | _snapshot_ref_date | DATE      | YES
(6 row(s))
```

Both tables agree on all three types, which is what lets the gate's future
append match by name.

---

## 9. UC planned the staging overwrite as a table REPLACE, not `OverwriteByExpression`

```sql
SELECT version, operation FROM (DESCRIBE HISTORY workspace.default.bronze_cnpj_estab_staging) ORDER BY version
```
```
version | operation
--------+-----------------------------------
0       | CREATE TABLE
1       | STREAMING UPDATE
2       | STREAMING UPDATE
3       | SET TBLPROPERTIES
4       | COMPUTE STATS
5       | ADD COLUMNS
6       | CREATE OR REPLACE TABLE AS SELECT
(7 row(s))
```

Version 6 is `CREATE OR REPLACE TABLE AS SELECT`, with
`"isV1SaveAsTableOverwrite":"true"` in its `operationParameters`.
`_assert_constraints`' own docstring documents this branch as unknowable from
the client — which of `OverwriteByExpression` (keeps metadata) or
`AtomicReplaceTableAsSelect` (drops CHECK constraints and NOT NULL) Unity
Catalog picks for `mode("overwrite").saveAsTable` "is not observable from here
without running it against the workspace." This run is what ran it against the
workspace, against a real 71.9M-row managed UC table: **UC picked the
replace.**

Table properties survived it:

```sql
SHOW TBLPROPERTIES workspace.default.bronze_cnpj_estab_staging
```
```
delta.enableDeletionVectors                    | true
delta.enableRowTracking                        | true
delta.parquet.compression.codec                | zstd
delta.workloadBasedColumns.deltaFileStatistics | `_batch_id`
... (15 row(s))
```

`delta.workloadBasedColumns.deltaFileStatistics` was set in version 3
(`SET TBLPROPERTIES`) and survived version 6's replace intact. Staging carries
no CHECK constraint and no column mask, so nothing was lost here, and
`_assert_constraints`'s skip on staging ([§3.1](#31-constraint-ddl-was-being-applied-to-whichever-table-the-script-wrote-not-only-to-bronze))
cost nothing.

**The consequence, stated plainly:** a CHECK constraint, a NOT NULL, and a UC
column mask are not table properties, and are not carried through a replace the
way the property above was. Quarantine's complete history is two versions —
v0 `CREATE TABLE AS SELECT`, v1 `ADD COLUMNS` — with no third, write-shaped
commit ([§6](#6-the-quarantine-run)). So on the one target where
`fill_and_overwrite` ([§3.2](#32-the-done-line-claimed-a-new-delta-version-unconditionally-even-on-a-run-that-committed-nothing))
skips the write entirely, no replace happened: **the 0-row skip prevented a
real table replace, not a hypothetical one.** Had the pre-fix behavior in
either §3.1 or §3.2 reached a masked or constrained non-bronze target, this is
the mechanism that would have executed against it.

This is now a measured, durable fact about how this repository's overwrites
behave against a managed UC table — not only about this migration. Whether it
deserves its own ADR, and whether it graduates beyond this run-evidence doc, is
an open question left for Task 7 to decide; it is recorded here rather than
left to be re-derived.

---

## 10. Cleanup, reverted and redeployed — confirmed in the deployed state

Commit `3453388` removes `databricks/resources/backfill_snapshot_columns_job.yml`
and the temporary `sync.paths` entry.

```
$ git diff 1cf3ea2 --stat -- databricks/databricks.yml
(empty = identical to 1cf3ea2)
```
```
$ databricks bundle validate -t free --profile opl-free
Name: open-payments-lakehouse
Target: free
Workspace:
  User: <user>
  Path: /Workspace/Users/<user>/.bundle/open-payments-lakehouse/free

Validation OK!
```

Redeployed after reverting — a committed revert alone would not have taken the
moved-sync-root layout out of the workspace:

```
$ cd databricks && databricks bundle deploy -t free --profile opl-free
Building opl_wheel...
Uploading ../dist/open_payments_lakehouse-0.0.0-py3-none-any.whl...
Uploading bundle files to /Workspace/Users/<user>/.bundle/open-payments-lakehouse/free/files...
Deploying resources...
Updating deployment state...
Deployment complete!
```

Verified in the workspace, not from the deploy's own success line — note the
`Uploading ../dist/...` path, itself the sync root having moved back:

```
$ databricks workspace list ".../free/files" --profile opl-free
DIRECTORY  .../free/files/resources
DIRECTORY  .../free/files/src
FILE       .../free/files/databricks.yml

$ databricks jobs list --profile opl-free -o json | ...
   [dev <user>] opl-bronze-cnpj-empresas
   [dev <user>] opl-bronze-cnpj-estabelecimentos
   [dev <user>] opl-bronze-cnpj-lookup
   [dev <user>] opl-bronze-cnpj-socios
   [dev <user>] opl-repromote-triaged-batch
   [dev <user>] opl-smoke
backfill job present: False
```

No `scripts/` directory in the deployed tree, `src/` back at the top level, and
the temporary job absent from the workspace as well as from the bundle.

### Independently re-confirmed by the controller, after the migration

Everything below was re-checked separately, against warehouse
`13cf10c85b0f189d`, after the migration and after the cleanup deploy above —
not read from the implementer's own report, and included here as a second,
independent source for facts the sections above already established:

- `bronze_cnpj_estab_staging`: 37 columns, 71,874,448 rows, `null_month` 0,
  `null_ref` 0, exactly one distinct `_snapshot_month` (`2026-06`) paired with
  `_snapshot_ref_date` `2026-06-13`, and one distinct `_batch_id` — the same
  pre-existing batch preserved.
- `bronze_cnpj_estab_quarantine`: 38 columns, 0 rows. Its **complete** history
  is two versions: v0 `CREATE TABLE AS SELECT`, v1 `ADD COLUMNS` naming
  exactly `_snapshot_month string` and `_snapshot_ref_date date`. There is
  **no write commit** — the migration added columns and issued no overwrite.
- The column-set diff between staging and quarantine, run after both
  migrations, returns exactly one row: `quarantine_not_staging |
  _dq_reject_reason`.
- `bronze_cnpj_estab_staging` history: v5 `ADD COLUMNS`, v6
  `CREATE OR REPLACE TABLE AS SELECT`.
- The revert was confirmed in the **deployed** state, not merely locally:
  `databricks bundle validate -t free` returned `Validation OK!`; every job's
  `python_file` was back to `files/src/...` rather than
  `files/databricks/src/...`; `databricks/resources/` held only the six
  permanent job YAMLs; and `databricks jobs list` returned only the six real
  jobs, with no `opl-backfill-snapshot-columns`.
- Pre-migration state, measured before any of this ran: staging 35 columns
  with neither snapshot column; quarantine 36 columns with neither; the
  column-set diff showing `quarantine == staging + _dq_reject_reason` exactly.

Every one of these agrees with what the implementer's own report recorded
live; nothing above changes any number given in §1–§9 — it is a second,
separately-taken source for the same facts.

---

## 11. What this document does not establish

- **PR B's remaining tasks have not run.** The three 2026-07 ingests
  (estabelecimentos, empresas, socios), the DQ gating decision and the
  `delta.dataSkippingStatsColumns` statistics measurement are outside Task 1 and
  outside this document; it will be extended, not replaced, when they run. The
  2026-07 upload has since landed ([§13](#13-task-4--the-2026-07-upload)).
- **Task 5 Step 0 ([§14](#14-task-5-step-0--the-auto-loader-state-is-now-month-scoped))
  has not been exercised against the workspace.** The month-scoped state layout
  is argued from Spark's and Databricks' own documented semantics and pinned by
  unit tests; no stream has yet written a `_checkpoints/<month>/<table_key>`
  directory. Whether
  `spark.databricks.cloudFiles.checkSourceChanged` would have fired for a
  same-Volume, different-subdirectory change under directory-listing mode is
  **unmeasured**, and now unmeasurable through the normal flow — the layout change
  removes the condition that would have tripped it.
- **The `_assert_constraints` scoping and the `fill_and_overwrite` skip are
  exercised only against estab's staging and quarantine.** Neither has yet run
  against a masked table (`socios`), so [§9](#9-uc-planned-the-staging-overwrite-as-a-table-replace-not-overwritebyexpression)'s
  consequence — a skip preventing a real replace, not a hypothetical one — is
  demonstrated for an unmasked target. The masked case remains argued from the
  mechanism, not observed.
- **The NOT NULL half of bronze's constraint set is not addressed here at
  all**, because it never applies off bronze: staging and quarantine only ever
  get the two snapshot columns, and `_assert_constraints` now runs against
  neither.
- **Whether the UC replace-plan finding in §9 graduates into its own ADR is
  undecided**, left for Task 7.
- **Gate item 3's mis-specification ([§6](#6-the-quarantine-run)) was accepted
  on the implementer's judgement, not on a standing rule that says so.** If the
  standing rule is "any listed condition not met means BLOCKED regardless of
  mechanism," this run deviated from it once, documented in full, and did not
  stop.

---

## 12. Task 2 — ADR 0009's provenance guard, proved refusing a stale deploy before any downstream task started

Task 2 built the mechanism [ADR 0009](adr/0009-deployed-revision-provenance.md)
argues for: an `assert_deployed_revision` task, wired first in every ingestion
job and in `repromote_triaged_batch`, that compares a `revision` bound at run
time from the local repository against a SHA the deployed wheel stamps into
`opl/_revision.py` at build time. Why the check is shaped that way — run-time
binding, a generated module rather than a version string, the `+dirty` suffix,
the watched-path fix in `2396af3` — is argued in the ADR and is not restated
here. This section is the live proof against the Free Edition workspace, run
against `bronze_cnpj_socios`, `bronze_cnpj_lookup` and `opl_smoke` on
**2026-08-03** from branch `feat/f1-4b-pr-b-second-month`. As with
[§10](#10-cleanup-reverted-and-redeployed--confirmed-in-the-deployed-state),
quotes below are the implementer's own from the task-2 report unless marked
otherwise; independent confirmation is in
[§12.8](#128-independently-re-confirmed-by-the-controller).

### 12.1 Sequence, with run ids

| # | what | run / result |
|---|---|---|
| 1 | deploy at `2396af3` | `Deployment complete!` |
| 2 | smoke — what is deployed? | run `136918510113031` → `built_revision='2396af3...0952'` |
| 3 | wrong run-time revision (before `HEAD` moved) | run `501199236916061` → refused |
| 4 | commit `109ac6e` (`HEAD` moves, nobody deploys) | — |
| 5 | the incident shape | run `788625093349052` → refused, 8 tasks SKIPPED |
| 6 | deploy at `109ac6e` | `Deployment complete!` |
| 7 | smoke — caching discriminator | run `796168381085213` → `built_revision='109ac6e...e38b'` |
| 8 | clean pass, guard only | run `597331898363369` → TERMINATED SUCCESS |
| 9 | trap 3: no `--params` at all | run `785856150554768` → refused on the sentinel |

### 12.2 The refusal, and the ordering it proves

By step 5, `bronze_cnpj_socios` had been deployed at `2396af3`
(`2396af361702f645f51dab5d201f1c8271cf0952`). `HEAD` then moved to `109ac6e`
(`109ac6eee6550d4fd7c2f74d4c72a089177de38b`, *fix: the dirty-wheel refusal
stops claiming a second fault that is not there*) with nobody running
`bundle deploy` again, and the run was launched for the new `HEAD` —
deliberately reproducing F1.4b PR A's second incident (ADR 0009, Context: "a
socios re-run terminated SUCCESS having masked only bronze"). The launch:

```
$ databricks bundle run bronze_cnpj_socios -t free \
    --params month=2026-06,revision=$(git rev-parse HEAD)
Run URL: https://<workspace-host>/jobs/551983635654258/runs/788625093349052
```

Run `788625093349052`'s per-task result, from `databricks jobs get-run`:

```
task_key                   lifecycle   result           attempt   duration_ms
assert_deployed_revision   TERMINATED  FAILED           0         29000
assert_deployed_revision   TERMINATED  FAILED           1         16000
ensure_masked_table        SKIPPED     UPSTREAM_FAILED  0         0
unzip                      SKIPPED     UPSTREAM_FAILED  0         0
ingest                     SKIPPED     UPSTREAM_FAILED  0         0
dq_gate_batch              SKIPPED     UPSTREAM_FAILED  0         0
check_bad_rows             SKIPPED     UPSTREAM_FAILED  0         0
fail_on_dq                 SKIPPED     UPSTREAM_FAILED  0         0
promote                    SKIPPED     UPSTREAM_FAILED  0         0
reclaim_landing             SKIPPED     UPSTREAM_FAILED  0         0

tasks that actually STARTED: ['assert_deployed_revision']
```

Overall run state FAILED (lifecycle `INTERNAL_ERROR`, discussed in
[§12.3](#123-the-retry-that-should-not-have-happened)). Two things in that table
carry the weight and are called out rather than left for the reader to notice.
First, every task other than the guard is `UPSTREAM_FAILED` at `duration_ms 0`
— nothing downstream **started**, which is the ordering claim this task exists
to prove, not merely a fast failure: no DDL reached the masked socios tables,
no zip was expanded, no stream advanced a checkpoint. Second,
`assert_deployed_revision` itself appears **twice** — `attempt 0` (29000 ms)
and `attempt 1` (16000 ms) — covered next.

The refusal, verbatim:

```
WrongRevision: refusing to run: the deployed wheel was built from 2396af361702f645f51dab5d201f1c8271cf0952, and this run was launched for 109ac6eee6550d4fd7c2f74d4c72a089177de38b. Different commits -- so every task after this one would execute code that is not the code you are reading.
The likeliest cause is the simplest: nobody ran `databricks bundle deploy -t free` since 109ac6eee6550d4fd7c2f74d4c72a089177de38b. CI validates the repository and never what is deployed, so nothing else would have gone red. It can also be a stale wheel inside a fresh deploy -- the jobs receive the wheel through the glob `dependencies: ["../../dist/*.whl"]` over a dist/ that nothing cleans.
Deploy, then launch again with the same revision.
```

### 12.3 The retry that should not have happened

`assert_deployed_revision` declares `max_retries: 0`, and the platform ran it
twice anyway against the same run — `attempt 0` then `attempt 1`, both
`FAILED`. The cause: an uncaught exception leaves the task's lifecycle state
`INTERNAL_ERROR`, and `max_retries` governs clean task failures, not
`INTERNAL_ERROR` — a fact this repo already carries, in
`docs/adr/0008-pii-masking-socios.md:143` and in
`docs/f1.4a-migration-evidence.md`, §7.2 ("the failure triggered a retry past
`max_retries: 0`"), but never before observed on a task whose entire job is to
refuse.

It is harmless here, and the reason is structural rather than lucky: the guard
reads two strings and writes nothing, so it is idempotent by construction and
refused identically on both attempts. The durable consequence is not scoped to
this one guard: **any task placed first in a job as a guard must be
side-effect-free**, because `max_retries: 0` will not protect a task that
writes something and then gets retried by the platform against its own
declared setting.

### 12.4 Filename caching — ruled out by measurement

`pyproject.toml` declares a static `version = "0.0.0"`, so the wheel filename
(`open_payments_lakehouse-0.0.0-py3-none-any.whl`) never changes across
rebuilds. The concern: if serverless caches an install by filename, a redeploy
could leave the **old** stamped wheel installed under an unchanged name, and
every guarded job would refuse permanently — a state indistinguishable from a
broken guard.

The discriminator is `opl_smoke`, which reads `built_revision()` from the same
wheel, the same `environment_key` and the same
`dependencies: ["../../dist/*.whl"]` glob as every guarded task, and cannot
refuse because it carries no guard. Two runs bracket the second deploy: run
`136918510113031` reported
`built_revision='2396af361702f645f51dab5d201f1c8271cf0952'` before it, and run
`796168381085213` reported
`built_revision='109ac6eee6550d4fd7c2f74d4c72a089177de38b'` after it — the same
wheel filename, different content read back both times. No caching, so no
`dist/` emptying and no version bump were needed. This is a second reason
`opl_smoke` stays deliberately unguarded rather than merely exempt: it is the
one job that can answer "what is actually installed" without itself being
subject to the question it would need to answer.

### 12.5 A distinction claimed, then retracted

The controller's dispatch for this step asked for confirmation that the
wrong-revision refusal message is distinguishable from a stale-deploy message.
It is not, and cannot be: `_refuse_the_mismatch` branches on what the deployed
value **is** — a well-formed object name that fails to match — and a
genuinely stale deploy and an operator's mistyped `--params revision=`
produce exactly that shape. From inside the run both are the same fact,
expected != actual, and nothing reachable from a job can see the operator's
repository to tell them apart. The message is written accordingly, as a hint
rather than a determination — "The likeliest cause is the simplest" — naming
both revisions and stating a likeliest cause without asserting it.

The implementer's own report had implied, in its Step 6 planning notes, that
the message could make this distinction; the report retracts that in the same
section the live refusal is quoted in, rather than treating it as a limitation
discovered late. It is the second controller-authored specification defect
found this phase — the first was the verification gate specified against a
state that could not exist yet, already recorded in
[Gate item 3](#gate-item-3--specified-against-a-state-that-could-not-exist-yet).
In both cases the implementer reported the mis-specification instead of
manufacturing a result that would have satisfied it as written.

### 12.6 The clean pass

Deployed at `109ac6e`, launched at `109ac6e`, with `--only
assert_deployed_revision` so that proving the pass moved no 2026-06 data:

```
$ databricks bundle run bronze_cnpj_socios -t free --only assert_deployed_revision \
    --params month=2026-06,revision=$(git rev-parse HEAD)
Run URL: https://<workspace-host>/jobs/551983635654258/runs/597331898363369
assert_deployed_revision: OK -- the installed wheel was built from 109ac6eee6550d4fd7c2f74d4c72a089177de38b, which is the revision this run was launched for. That is a claim about the WHEEL; the entry-point files under databricks/src were synced by the same deploy, and a deploy made from a modified tree would have stamped +dirty.
```

The other tasks reported `SKIPPED / DISABLED, attempt 0` — present in the run
graph, never started, because `--only` was used.

### 12.7 What this does not cover

The `+dirty` refusal — the case where the deployed wheel is stamped from an
uncommitted tree — has been observed **locally only**. Proving it against the
workspace would mean deliberately deploying a wheel known to be built from a
dirty tree, which conflicts with leaving the workspace deployed clean at
`109ac6e` for the tasks that follow this one. That is a deliberate trade, not
an oversight: the `+dirty` path stays proven locally, through real
`uv build --wheel` invocations reading the stamp out of the built wheel
(fix round 1), and unobserved in the workspace.

### 12.8 Independently re-confirmed by the controller

Everything below was re-checked separately, against the Databricks Jobs API
directly (`/api/2.2/jobs/runs/get`, `/api/2.2/jobs/runs/get-output`) and
against real local wheel builds — not read from the implementer's own report,
and included here as a second, independent source for facts the sections
above already established, plus one probe the report did not run:

- Run `788625093349052` (socios, deployed at `2396af3`, launched for
  `109ac6e`): run state FAILED; the per-task result states and durations in
  [§12.2](#122-the-refusal-and-the-ordering-it-proves) reproduced exactly —
  every task but the guard `UPSTREAM_FAILED` at `dur=0`, `assert_deployed_revision`
  `FAILED` at `dur=29000` (attempt 0) and `dur=16000` (attempt 1); and the
  `WrongRevision` refusal message reproduced verbatim.
- Run `597331898363369` (the clean pass): run state SUCCESS;
  `assert_deployed_revision` SUCCESS at `dur=28000` — a number the
  implementer's own report did not print — and every other task `DISABLED` at
  `dur=0`.
- The local stamp mechanism, probed with a real `uv build --wheel` and the
  stamp read out of the built wheel: a clean tree stamps the bare SHA equal
  to `git rev-parse HEAD`; a dirty `src/opl/config.py` stamps `<sha>+dirty`;
  a dirty `databricks/src/bronze_ingest.py` also stamps `<sha>+dirty` — the
  hole a review found and `2396af3` closed, since the identical tree state
  stamped a bare, clean SHA one commit earlier. Restoring the modified file
  returns the stamp to bare.

Every one of these agrees with what the implementer's own report recorded
live; nothing above changes any number given in §12.1–§12.7 — it is a second,
separately-taken source for the same facts, plus the `src/opl/config.py` case
the report itself did not probe.

---

## 13. Task 4 — the 2026-07 upload

Task 4 uploaded the 30 2026-07 zips — ten each of empresas, estabelecimentos
and socios — to the landing Volume. **Commits: none.** No tracked file
changed, so nothing needed redeploying: `scripts/extract_giants.py` runs off
Databricks by design (two-layer topology, ADR 0002) and moves bytes through
the Files API with a `WorkspaceClient` built from the `opl-free` CLI profile.
There is no wheel on this path, no `revision` parameter and no
`assert_deployed_revision` task, so
[§12](#12-task-2--adr-0009s-provenance-guard-proved-refusing-a-stale-deploy-before-any-downstream-task-started)'s
guard was never exercised here. The workspace stayed deployed at `109ac6e`
and `HEAD` stayed at `4f31e7b` — a gap this task inherited rather than
widened, and Task 5 must close it with a `bundle deploy` before it launches
anything.

### 13.1 Step 0 — the headroom, and no quota figure exists

Before this task the Volume held 155 objects, 15,505,543,535 B, all under
`.../landing/cnpj/2026-06`; no `2026-07` directory existed and nothing had
been uploaded.

| | bytes | GB (1e9) |
|---|---|---|
| Volume before this task | 15,505,543,535 | 15.51 |
| + the 30 2026-07 zips | 7,343,546,309 | 7.34 |
| = Volume after this task | 22,849,089,844 | 22.85 |
| highest this Volume has demonstrably held | 22,003,959,144 | 22.00 |

The comparison row is not a guess. Before F1.4a's reclaim,
`cnpj/2026-06/estabelecimentos` held ten inner CSVs (16,743,815,717 B) beside
`cnpj/2026-06/zips/estabelecimentos`'s ten zips (5,259,919,847 B) and
223,580 B of lookups, and `docs/f1.3-estabelecimentos-run-evidence.md:97-99`
records that footprint as having run "inside Databricks Free Edition fair use
without a single warning or a disabled-compute event, across two calendar
days." This task's upload lands 845,130,700 B — 3.8% — above that footprint:
an argument from precedent, not from a checked-and-cleared limit.

**No quota figure is exposed by any API, and this is stated plainly rather
than implied checked.** The UC volume API
(`/api/2.1/unity-catalog/volumes/workspace.default.landing`) returns catalog
and schema identity, ownership, `volume_type: MANAGED`,
`securable_kind: VOLUME_DB_STORAGE` and a `storage_location` — no size, usage
or quota field of any kind, on Free Edition. So "the upload fits" here means
fits a footprint this Volume has already carried without a quota event, never
fits under a published limit, because no limit is published.

**Independently confirmed by the controller:** querying the Volume directly
returned all 30 objects landed under
`.../cnpj/2026-07/zips/{empresas,estabelecimentos,socios}/`, ten each, with
per-group byte sums exactly equal to local disk — empresas 1,360,217,689 B,
estabelecimentos 5,298,914,388 B, socios 684,414,232 B, total
7,343,546,309 B — reproducing the 22,849,089,844 B post-upload total above to
the byte. Full per-object figures are in
[§13.4](#134-verification-all-30-objects-byte-for-byte).

What Task 5 walks into once 2026-07 is unzipped is carried in
[§13.8](#138-for-task-5--the-48-gb-peak).

### 13.2 Step 0b — nothing was deleted, and that is the finding

The controller's instruction for this step was to free the ten 2026-06
estabelecimentos zips only if the headroom measurement in
[§13.1](#131-step-0--the-headroom-and-no-quota-figure-exists) required it, and
not to delete for tidiness. **It did not require it.** §13.1's own arithmetic
is 22.85 GB against 22.00 GB this Volume has already carried — 3.8% above a
demonstrated footprint, not proximity to a published limit, because no quota
figure was checked or cleared; there was none to check.

**The stronger half of the finding is that the controller's own "pure
residue" premise for these ten objects does not survive contact with the
repo's own record.** The reasoning that estabelecimentos zips whose CSVs are
already reclaimed are residue by construction is not what F1.4a's own
evidence says about exactly these ten objects — `docs/f1.4a-migration-evidence.md:500-502`
records F1.4a considering them and deliberately keeping them:

> The zips are the only way back to the source if a parse defect is found
> after ingestion, which happened twice in F1.3. They stay, at roughly a
> third of the bytes.

That is a documented decision, not an oversight, and the two F1.3 parse
defects it cites are its own evidence. These ten objects are a retained
rollback path, not leftovers nobody thought about — reversing that decision
is legitimate under real space pressure, but not inside a task whose job is
an upload, on tidiness grounds, when the measurement says the upload fits.

The four legs of the safe-to-delete argument were verified anyway, so Task 5
can act on them without re-deriving anything: (1) the CSVs are already
reclaimed — `cnpj/2026-06/estabelecimentos` lists 0 entries; (2) bronze holds
the rows — `bronze_cnpj_estabelecimentos` has 71,874,448 rows over 10
distinct `_source_file`; (3) staging holds the same batch —
`bronze_cnpj_estab_staging`, identical row and file counts; (4) the RFB can
still re-serve the files — a live PROPFIND of the 2026-06 share directory
returned all ten Estabelecimentos zips, every size matching the object in the
Volume exactly, 5,259,919,847 B total both sides. Leg 4 does weaken F1.4a's
"only way back" wording — the share still serves 2026-06 byte-for-byte — but
that is what makes the delete available to Task 5, not a reason to spend it
now. The four legs, the 5,259,919,847 B figure and the path are handed
forward in [§13.8](#138-for-task-5--the-48-gb-peak).

### 13.3 The run, and the timing that beat both predictions

Run under Task Scheduler against a `.cmd` wrapper, never `Start-Process`. The
three groups ran sequentially, smallest first — socios, empresas,
estabelecimentos — so each group's wall clock is a clean measurement rather
than three transfers sharing one link, and so the single-PUT sample landed
early enough to sanity-check the rate while the 5.3 GB estabelecimentos group
was still ahead. A plumbing probe (`--parts 9`, no `--upload`) proved the
scheduled-task session, `uv` resolution, the working directory and the
WebDAV auth without writing a byte to the Volume, returning `rc=0` in 3.3 s.

The run was restarted once, deliberately: the first, uninstrumented launch
logged nothing about upload geometry, because `databricks.sdk.mixins.files`
emits part size, part count, every part's byte range and the batch decision
only at DEBUG. After three socios objects had already uploaded, the run was
stopped, the three objects were listed, quoted and deleted, the tree was
confirmed empty, and the run was relaunched with a scratch `sitecustomize.py`
on `PYTHONPATH` turning that logger on (more on which in
[§13.9](#139-the-geometry-instrumentation-is-not-in-the-repo)). No tracked
file was touched by any of this, so every one of the 30 objects verified in
[§13.4](#134-verification-all-30-objects-byte-for-byte) was written by the
single run whose timings follow, not inherited from the aborted attempt.

| | bytes | wall clock | MB/min |
|---|---|---|---|
| Socios | 684,414,232 | 19 m 35.5 s | 34.9 |
| Empresas | 1,360,217,689 | 10 m 44.0 s | 126.7 |
| Estabelecimentos | 5,298,914,388 | 29 m 25.2 s | 180.1 |
| **whole run** | **7,343,546,309** | **59 m 44.7 s** | **122.9** |

**The whole run took 59 m 45 s** (59 m 44.66 s precisely), faster than both
predictions carried into this task: **≈87 min** from PR A's per-path rates
and **≈107 min** from PR A's blended rate. Reproduced from the same numbers:
6,899.105510 MB of multipart payload / 94.7 MB/min + 444.440799 MB of
single-PUT payload / 24.8 MB/min = 90.8 min per-path, and
7,343.546309 MB / 65.5 MB/min = 112.1 min blended — the small gap between
these and the ≈87/≈107 the plan and brief quoted most likely comes from where
`Socios0` and `Empresas0` are placed, since PR A deliberately excluded both
from its two nine-object per-path subsets.

**The reason the run beat both predictions is
[§13.6](#136-rates--the-multipart-advantage-and-why-single-put-is-not-a-bandwidth-number)'s
finding, not a happy surprise.** Both predictions were built from PR A's
rates — 94.7 MB/min multipart, 24.8 MB/min single-PUT — and this run's
multipart side moved 1.74× faster than that, at 164.9 MB/min over 20
objects, while single-PUT reproduced PR A's rate exactly. A prediction built
from a rate that turned out to be 1.74× conservative on the larger, faster
side of the payload was always going to land early; the time is a
consequence of §13.6's finding, not an independent surprise.

### 13.4 Verification: all 30 objects, byte-for-byte

Landed sizes read back from the Volume, compared against `os.path.getsize`
on local disk. **Delta 0 on all 30**, and every object resolved at the path
the uploader's own `spec_for_contract(...).subdir` produces:

| object | bytes | path taken | part size | parts | batch |
|---|---|---|---|---|---|
| Socios0.zip | 239,973,433 | multipart | 10 MiB | 23 | 5 |
| Socios1.zip | 49,645,387 | single PUT | — | — | — |
| Socios2.zip | 49,205,177 | single PUT | — | — | — |
| Socios3.zip | 49,224,762 | single PUT | — | — | — |
| Socios4.zip | 49,588,399 | single PUT | — | — | — |
| Socios5.zip | 49,481,449 | single PUT | — | — | — |
| Socios6.zip | 49,250,249 | single PUT | — | — | — |
| Socios7.zip | 49,148,909 | single PUT | — | — | — |
| Socios8.zip | 49,787,076 | single PUT | — | — | — |
| Socios9.zip | 49,109,391 | single PUT | — | — | — |
| Empresas0.zip | 544,290,225 | multipart | 10 MiB | 52 | 8 |
| Empresas1.zip | 77,888,891 | multipart | 10 MiB | 8 | 3 |
| Empresas2.zip | 79,116,294 | multipart | 10 MiB | 8 | 3 |
| Empresas3.zip | 85,108,747 | multipart | 10 MiB | 9 | 3 |
| Empresas4.zip | 90,243,240 | multipart | 10 MiB | 9 | 3 |
| Empresas5.zip | 97,345,876 | multipart | 10 MiB | 10 | 4 |
| Empresas6.zip | 94,246,070 | multipart | 10 MiB | 9 | 3 |
| Empresas7.zip | 98,748,860 | multipart | 10 MiB | 10 | 4 |
| Empresas8.zip | 98,830,257 | multipart | 10 MiB | 10 | 4 |
| Empresas9.zip | 94,399,229 | multipart | 10 MiB | 10 | 4 |
| **Estabelecimentos0.zip** | **2,164,567,397** | multipart | **50 MiB** | **42** | 7 |
| Estabelecimentos1.zip | 341,578,424 | multipart | 10 MiB | 33 | 6 |
| Estabelecimentos2.zip | 336,376,100 | multipart | 10 MiB | 33 | 6 |
| Estabelecimentos3.zip | 367,052,398 | multipart | 10 MiB | 36 | 6 |
| Estabelecimentos4.zip | 340,421,684 | multipart | 10 MiB | 33 | 6 |
| Estabelecimentos5.zip | 336,378,631 | multipart | 10 MiB | 33 | 6 |
| Estabelecimentos6.zip | 368,109,911 | multipart | 10 MiB | 36 | 6 |
| Estabelecimentos7.zip | 340,694,903 | multipart | 10 MiB | 33 | 6 |
| Estabelecimentos8.zip | 334,764,377 | multipart | 10 MiB | 32 | 6 |
| Estabelecimentos9.zip | 368,970,563 | multipart | 10 MiB | 36 | 6 |

```
group Empresas             objects= 10 bytes=1360217689
group Estabelecimentos     objects= 10 bytes=5298914388
group Socios               objects= 10 bytes=684414232
TOTAL verified objects=30 bytes=7343546309
```

For the 21 multipart objects the observed per-part byte ranges have indices
exactly `1..N`, are contiguous with no gap or overlap, and sum to exactly the
local file size — **505 part uploads observed** (23 socios + 135 empresas +
347 estabelecimentos), all 21 tiling their file exactly. The 9 single-PUT
objects are exactly `Socios1.zip`…`Socios9.zip`, each below the
52,428,800 B floor; every Empresas/Estabelecimentos object and `Socios0.zip`
are above it and went multipart, as predicted.

**Independently confirmed by the controller:** the per-group byte sums above
— empresas 1,360,217,689 B, estabelecimentos 5,298,914,388 B, socios
684,414,232 B, total 7,343,546,309 B — were reproduced from a direct query
of the Volume, matching local disk exactly.

### 13.5 The Estabelecimentos0.zip geometry — 42 parts, not 41

```
16:18:12.461 SDK Optimized part size for upload: 52428800 bytes for content length 2164567397 bytes
16:18:12.461 SDK Upload context: part_size=52428800, batch_size=7, content_length=2164567397
16:18:13.871 SDK Uploading part 1: [0, 52428799]
16:20:24.662 SDK Starting 10 worker threads for parallel upload
...
16:26:51.843 SDK Uploading part 41: [2097152000, 2149580799]
16:28:06.678 SDK Uploading part 42: [2149580800, 2164567396]
```

**50 MiB parts, 42 of them, 10 concurrent threads, presigned URLs fetched 7
at a time.** Part 42 is the 14,986,597 B tail; parts 1-41 are 52,428,800 B
each. The ranges tile `[0, 2164567396]` exactly, and the landed object is
2,164,567,397 B — all 42 parts committed, not a subset. 651.6 s; the largest
object this repo has uploaded to date.

**Independently confirmed by the controller:**
`2,164,567,397 / 52,428,800 = 41.2858`, ceiling **42** — one division,
matching the observed part count exactly.

This corrects [ADR 0007](adr/0007-multipart-upload.md)'s geometry table,
which lists 41 parts for `Estabelecimentos0.zip` — a count specific to
2026-06's byte count (2,128,818,559 B) that does not generalise to 2026-07's
(2,164,567,397 B). **Both the F1.4b PR B plan and this task's own controller
brief carried "41 parts" forward** as the prediction for this run, so a
wrong number in an ADR propagated into two downstream documents before this
measurement caught it. The part *size* (50 MiB, the smallest option giving
≤ 100 parts) generalises across months; the part *count* does not.

The same table's `batch_size` gap is also largest on this object:
`files_ext_multipart_upload_batch_url_count = 1` argues, in its own comment,
for requesting one presigned URL at a time to avoid expiry on a non-seekable
stream; the observed batch here was **7**, and
`_get_optimized_performance_parameters_for_upload` computes it as
`ceil(sqrt(part_num))` unconditionally, overriding that default whenever the
content length is known. Nothing expired across this run's 505 parts, so
this is a documentation gap and a latent risk, not an incident — recorded in
ADR 0007's corrected Consequences section.

### 13.6 Rates — the multipart advantage, and why single-PUT is not a bandwidth number

| subset | objects | bytes | wall clock | MB/min | PR A |
|---|---|---|---|---|---|
| multipart | 20 | 6,530,134,947 | 2,376.7 s | **164.9** | 94.7 |
| single PUT | 9 | 444,440,799 | 1,077.1 s | **24.8** | 24.8 |

**Single-PUT reproduced PR A exactly: 24.8 MB/min**, on a different day, on
near-identical bytes (444,440,799 B here in 1,077.1 s; 444,421,834 B in PR A
in 1,076 s), flat at 23.8-25.1 MB/min per object across all nine.
**Multipart ran 1.74× faster than PR A observed** — 164.9 MB/min against
94.7, peaking at 199.3 MB/min on `Estabelecimentos0.zip` and 197.8 on
`Empresas0.zip`; the largest objects were the fastest.

**So the multipart advantage over single PUT was 6.6× this run, not the
3.8× ADR 0007's figures imply** (164.9 / 24.8 = 6.65). And since the same
link, session and client moved multipart bytes at up to 199.3 MB/min while
single PUT held at 24.8 both here and in PR A, **single PUT's 24.8 MB/min is
not a bandwidth limit — it is a stable per-request property**, observed to
three significant figures across two runs on different data. That is a
stronger statement than either measurement alone supports, and it changes
what ADR 0007's 94.7/24.8/3.8× figures mean: they describe PR A's sample,
not this link's throughput generally. This corrects ADR 0007's Consequences
section, whose bullet reads the 3.8× figure as though it generalises.

### 13.7 Per-part retry — the opportunity was taken, and closed nothing

742 log lines, 505 part uploads across the 21 multipart objects: zero
retries, zero URL expiries, zero warnings, zero `Falling back`, zero cleanup
lines. ADR 0007 closed its retry discussion with "PR B's 7.343 GB is the next
opportunity" for observing per-part retry recovery. **PR B has now run, and
the opportunity produced no failure** — so the item is not closed by this
run, it is spent without an answer. Across both PRs that is 9.376 GB and
every part upload this repo has ever performed, with nothing having
retried. A link that keeps not failing is not the same evidence as an
observed recovery; closing this for real needs a deliberate fault injection,
not another large upload. This updates ADR 0007's open item, which otherwise
reads as though the next opportunity is still ahead rather than already
spent.

### 13.8 For Task 5 — the ~48 GB peak

```
22,849,089,844  after this task
+25,122,753,704  2026-07 unzipped
=47,971,843,548  peak  (47.97 GB / 44.68 GiB)
```

**2.18×** the highest footprint this Volume is known to have held
(22,003,959,144 B, [§13.1](#131-step-0--the-headroom-and-no-quota-figure-exists)),
against no published limit — this task's 3.8% margin over precedent does not
scale to Task 5's. **Task 5 should sequence group by group, with a reclaim
between, rather than unzipping all three groups and hoping.** Reclaiming
each group's CSVs before unzipping the next keeps the peak well below the
48 GB figure; unzipping all three first does not.

The verified-safe delete from
[§13.2](#132-step-0b--nothing-was-deleted-and-that-is-the-finding) is
available if pressure materialises: 5,259,919,847 B at
`/Volumes/workspace/default/landing/cnpj/2026-06/zips/estabelecimentos/`, all
four legs already checked there, outside `reclaim_landing`'s reach by
construction (`src/opl/bronze/retention.py:27-33`), so it is a hand-run
delete rather than one `reclaim_landing` can perform on its own.

### 13.9 The geometry instrumentation is not in the repo

The per-part geometry in
[§13.5](#135-the-estabelecimentos0zip-geometry--42-parts-not-41) and the
`batch_size` observation came from turning on
`databricks.sdk.mixins.files`'s DEBUG logger through a scratch
`sitecustomize.py` on `PYTHONPATH`, by design: it is untracked scratchpad,
not a repo change, so this task committed nothing and needed no redeploy.
That means the observation is reproducible in method — point `PYTHONPATH` at
a directory holding only that file — but not committed; nothing in this
repo turns this logging on today. Whether it should become a documented
opt-in is left open rather than decided here, since that would turn this
task into a code change requiring its own redeploy.

---

## 14. Task 5 Step 0 — the Auto Loader state is now month-scoped

**No run in this section. This is a code change made *before* the 2026-07
ingests, and the operational consequence it creates is the reason it is in this
document rather than only in a task report.**

### 14.1 What was wrong, and what changed

`src/opl/bronze/autoloader.py` keyed both `schema_location` and
`checkpoint_location` on `table_key` alone, while the stream's source path is
month-scoped (`DEFAULT.landing_table(spec.subdir, month)`). Every ingest through
2026-06 ran under that layout, so it was never exercised: PR A's empresas and
socios streams were first runs. The first 2026-07 ingest would therefore have
restarted a live streaming query against a **different source directory** — which
Spark's "Recovery Semantics after Changes in a Streaming Query" lists as
"generally not allowed as the results are unpredictable", defining *not allowed*
as "the restarted query is likely to fail with unpredictable errors". Databricks
states the positive rule for Auto Loader directly: more than one source data
location loaded into one target table "requires a separate streaming checkpoint".

The locations are now `_checkpoints/<month>/<table_key>` and
`_schemas/<month>/<table_key>` — **siblings of**, never children of, the existing
`_checkpoints/<table_key>` directories, so nothing new is written inside a
checkpoint directory a 2026-06 query still owns.

Two things this is deliberately **not**:

- It is **not** the silent-success case `registry.py`'s checkpoint-namespace guard
  warns about. Auto Loader's exactly-once tracking is keyed on the full file path,
  and 2026-07's paths are all new, so a query that starts ingests them correctly.
  The risk was failure at startup, not an under-ingest reported as SUCCESS.
- It is **not** resting on `spark.databricks.cloudFiles.checkSourceChanged`. That
  guard is active and its trip is a loud `[STREAM_FAILED]`
  `StreamingQueryException`, but the documented example is a cross-*bucket*
  mismatch under file-notification mode. Whether it fires for a same-Volume,
  different-subdirectory change under the directory-listing default this job uses
  is **not stated by the docs and has not been measured here**. The layout was
  fixed instead of the guard relied on.

`Trigger.AvailableNow` changes none of the above.

### 14.2 ⚠️ 2026-06 must not be re-run through the ingestion flow

The 2026-06 state at the old, unscoped paths is now **orphaned**: nothing reads
it, and it is left exactly where it is, because streaming state is never
migrated. The cost of that decision is the one thing an operator has to carry
forward:

> A 2026-06 ingest launched through the job flow now finds an **empty**
> month-scoped checkpoint, treats every 2026-06 file as new, and appends that
> month's rows into staging a second time under a fresh `_batch_id`. The
> promote's idempotence is keyed on `_batch_id`, so it **cannot see the
> duplication** and will carry it into bronze. Nothing fails. The row counts
> double.

What does and does not protect against this:

- **Not** `max_retries: 0`, and not the platform. A retry past `max_retries: 0`
  on `INTERNAL_ERROR` is already observed twice in this repo
  ([§12.3](#123-the-retry-that-should-not-have-happened),
  `docs/f1.4a-migration-evidence.md` §7.2).
- **Partly** the `revision` job parameter. Every ingestion job defaults `month`
  to `2026-06`, so a bare `databricks bundle run bronze_cnpj_<table> -t free`
  would target exactly the month at issue — but `revision` defaults to
  `REQUIRED-PASS-A-REVISION`, which `assert_deployed_revision` refuses first
  (ADR 0009). So the accident needs a deliberate `--params month=2026-06` with a
  valid revision; it is not one keystroke away.
- **Nothing else.** There is no code guard that refuses an already-ingested
  month. Re-running a month that has already been promoted is a manual,
  deliberate act, and it requires deciding what to do about the duplicate batch
  before launching, not after.

The same statement lives on `checkpoint_location`'s own docstring, which is where
a reader who wonders why an unread `_checkpoints/bronze_cnpj_lookup` still exists
in the Volume will arrive.

### 14.3 What Steps 1–6 should watch for

- **Every 2026-07 ingest is a FIRST run against an empty checkpoint**, by
  construction. The first `ingest` task's duration is therefore a cold-start
  number with no prior offsets to skip, and there is nothing to compare it
  against from 2026-06.
- **Nothing should appear under a 2026-06 state path during Step 2.** After the
  empresas run, `_checkpoints/` should hold the pre-Step-0 `<table_key>`
  directories untouched plus a new `2026-07/` directory, and no `2026-06/`
  directory at all. A `2026-06/` appearing there means a month reached the
  checkpoint that did not reach the source path.
- **Empresas first is now load-bearing, not incidental.** It is the smallest of
  the three, so this layout decision is validated before ~25 GB of
  estabelecimentos is read under it. Keep that order for that reason.

---

## 15. Task 5 — the three 2026-07 ingests, live

Steps 1–6 ran the three 2026-07 ingests — empresas, socios, estabelecimentos —
from branch `feat/f1-4b-pr-b-second-month` at `HEAD` `534ea92` against the live
Free Edition workspace on **2026-08-03**. No commit was made between the deploy
below and the last run. Times quoted from the Jobs API below are **UTC**; times
quoted from `databricks fs ls -l` are **local (UTC-3), rendered with a
misleading `Z` suffix**. The implementer used that three-hour offset to match
file timestamps to run ids; anyone who reads those listings as UTC is three
hours out. Not documented anywhere in this repo before now.

### 15.1 Deploy, verified by artefact rather than by its success line

The workspace was deployed at `109ac6e`
([§12](#12-task-2--adr-0009s-provenance-guard-proved-refusing-a-stale-deploy-before-any-downstream-task-started));
`HEAD` had since moved to `534ea92` through Task 5 Step 0 and its fix round.
`dist/` was emptied first, deliberately — the jobs install through the glob
`dependencies: ["../../dist/*.whl"]` over a directory nothing cleans, and the
wheel filename never changes (`version = "0.0.0"`), so a stale wheel is
invisible by name:

```
$ rm -f dist/*.whl && cd databricks && databricks bundle deploy -t free
Building opl_wheel...
Uploading ../dist/open_payments_lakehouse-0.0.0-py3-none-any.whl...
Deploying resources...
Deployment complete!
```

Four independent checks, none of which is the deploy's own success line:

```
deployed bytes: 90204          local bytes: 90204
RESULT: DEPLOYED WHEEL IS BYTE-IDENTICAL TO THE LOCAL BUILD
REVISION = '534ea9267ae2274b0dd2764576d196bfb340a0f7'      (inside the DEPLOYED wheel)
bronze_ingest.py:        IDENTICAL to HEAD's tree (3 x month=month present)
bronze_lookup_ingest.py: IDENTICAL to HEAD's tree (2 x month=month present)
```

`REVISION` carries no `+dirty`, so the tree was clean at build time. The two
entry points matter separately from the wheel: they are synced files, not
wheel contents, and they are where Task 5 Step 0's `month=month` wiring lives.

Then the one check that speaks for the *installed* wheel, not the uploaded
file — `opl_smoke`, unguarded by design
([§12.4](#124-filename-caching--ruled-out-by-measurement)), so it can answer
"what is actually installed" without being subject to the question:

```
$ databricks bundle run opl_smoke -t free
opl_smoke OK | catalog=workspace schema=default volume_root=/Volumes/workspace/default/landing
              month=2026-06 built_revision='534ea9267ae2274b0dd2764576d196bfb340a0f7'
```

Filename caching ruled out by measurement, as in
[§12.4](#124-filename-caching--ruled-out-by-measurement). `assert_deployed_revision`
then passed live at the head of all three ingest runs and both repromotes below.

### 15.2 Pre-run baselines, taken before anything ran

| table | bronze (2026-06) | staging | quarantine |
|---|---|---|---|
| empresas | 68,629,147 | 68,629,148 | 1 |
| socios | 27,836,651 | 27,838,448 | 1,797 |
| estabelecimentos | 71,874,448 | 71,874,448 | 0 |

Every row's arithmetic closes: staging = bronze + quarantine, one `_batch_id`
and one `_snapshot_month` (2026-06) per table. Landing Volume footprint
**28.24 GB**, with `_checkpoints/` and `_schemas/` each holding exactly the
four unscoped `<table_key>` directories from before Task 5 Step 0, and no
month directory yet.

### 15.3 The month-scoped checkpoint check — the gate on everything after it

[§14](#14-task-5-step-0--the-auto-loader-state-is-now-month-scoped) predicted a
layout; this is the run that tested it. Presence alone cannot distinguish "the
old checkpoint was left alone" from "it was read and rewritten under the same
file names", so the check was asked for as presence/absence and answered with
**timestamps**.

Every file in all four pre-Step-0 unscoped directories, after all three ingest
runs:

```
_checkpoints/bronze_cnpj_empresas  metadata 2026-08-01T14:07:00  offsets/0,1 2026-08-01T14:07:04-05  commits/0,1 2026-08-01T14:11:16-18
_checkpoints/bronze_cnpj_socios    metadata 2026-08-01T14:16:06  offsets/0,1 2026-08-01T14:16:09     commits/0,1 2026-08-01T14:17:43-44
_checkpoints/bronze_cnpj_estab     metadata 2026-07-28T16:51:52  offsets/0,1 2026-07-28T16:51:56-57  commits/0,1 2026-07-28T16:56:54-57
_checkpoints/bronze_cnpj_lookup    metadata 2026-07-31T11:19:31
```

Nothing dated 2026-08-03. The new state went to the month-scoped paths
instead:

```
_checkpoints/        -> 2026-07  bronze_cnpj_empresas  bronze_cnpj_estab  bronze_cnpj_lookup  bronze_cnpj_socios
_checkpoints/2026-07/ -> bronze_cnpj_empresas  bronze_cnpj_socios  bronze_cnpj_estab
_schemas/2026-07/     -> bronze_cnpj_empresas  bronze_cnpj_socios  bronze_cnpj_estab
```

Old unscoped dirs **untouched**, proven by mtime rather than by presence;
**`2026-07/` created** as a sibling of the four `<table_key>` dirs, not inside
any of them; and **no `2026-06/`** under either `_checkpoints/` or
`_schemas/` — the one outcome that would have meant a month reached the
checkpoint that diverged from the month reaching the source path, exactly the
failure [§14.1](#141-what-was-wrong-and-what-changed)'s
`_assert_source_dir_is_this_months` guard exists to prevent. It did not appear
for any of the three tables.

The estab checkpoint directory is `bronze_cnpj_estab`, not
`bronze_cnpj_estabelecimentos` — `table_key` is its own registry field and
does not track the table name; confirmed against the registry before looking,
rather than inferred from the listing.

### 15.4 `fail_on_dq` retried past `max_retries: 0`, on all three tables

The DQ gate fired on every one of the three ingests — empresas 1 reject,
socios 1,786, estabelecimentos 4 — so all three runs ended `INTERNAL_ERROR
FAILED` by design: `fail_on_dq` raises when the gate rejects anything, and an
uncaught exception makes the lifecycle `INTERNAL_ERROR`, which the platform
retried once on every run **despite `max_retries: 0`**. This is the same
behaviour [§12.3](#123-the-retry-that-should-not-have-happened) recorded for
`assert_deployed_revision` — now observed a second time, on a different task,
in the same phase. It is a general fact this repo already knew twice over
(also `docs/f1.4a-migration-evidence.md` §7.2), and it is harmless here for
the same structural reason as before: the retried task writes nothing —
`fail_on_dq` only inspects `check_bad_rows`'s outcome and raises, so a second
attempt raises identically rather than compounding any state.

---

## 16. The reclaim finding — eight instances, two executions, zero bytes ever deleted

Surfaced by an observation that did not fit, not sought out: 2026-06's
`empresas/` (5.36 GB) and `socios/` (2.85 GB) extracted CSVs from PR A were
still in the landing Volume, dated 2026-08-01, although PR A ran both jobs and
every ingestion job wires `reclaim_landing` after `promote`. Rather than infer
from the files' presence, every `reclaim_landing` task instance in this
workspace's history was enumerated — all jobs, 25 runs each — and the two that
executed were read for what they printed.

**Eight instances. Two executions. Zero bytes deleted.**

| outcome | n | which |
|---|---|---|
| `SKIPPED / EXCLUDED` — the gate fired, so `promote` was skipped and reclaim with it | 3 | empresas 2026-06, empresas 2026-07, socios 2026-06 run 1 |
| `SKIPPED / DISABLED`, `SKIPPED / UPSTREAM_FAILED` | 2 | socios `--only` run, socios provenance-refusal run |
| `TERMINATED / SUCCESS`, having deleted nothing | 2 | socios 2026-08-01T21:53:47Z, 2026-08-01T21:57:30Z |
| `SKIPPED / EXCLUDED` (socios, this phase) | 1 | socios 2026-07 |

The two executions printed, verbatim:

```
reclaim_landing: workspace.default.bronze_cnpj_socios holds no row of batch 451552926872284
-- nothing is proven persisted, so NOTHING WAS DELETED and the landed files stay.
One of: (a) this run's ingest found no new file for socios, the flow's legitimate
no-op, and there is genuinely nothing to reclaim; (b) the batch id names no batch this
table ever ingested ...; (c) bronze was rebuilt after the promote ...
```

Case (a) is the true one here: both runs re-ran `unzip` (which size-skips),
then `ingest` drained nothing because an earlier run's checkpoint had already
consumed those files, so their batches were empty and reclaim correctly
refused.

An earlier inference retracted before it stood: 2026-06's empty
`estabelecimentos/` directory looked like reclaim working. It is not — that
run (`118868417561350`) has **no `reclaim_landing` task at all**; its task
list is unzip, ingest, dq_gate_batch, check_bad_rows, promote, fail_on_dq,
because it predates the task's addition. Those CSVs went by hand, as F1.3's
own evidence records for the checkpoint directories of that era.

### 16.1 The mechanism is wiring, not `retention.py`

1. `reclaim_landing` is `depends_on: promote`; `promote` is `depends_on:
   [check_bad_rows, outcome: "true"]`. **Any batch with at least one reject
   never reaches reclaim.**
2. The batch that *does* hold the rows is then promoted by
   `repromote_triaged_batch`, whose task graph is only
   `assert_deployed_revision` and `promote` — **no reclaim task**, confirmed
   in the job YAML.
3. A later clean run cannot compensate: its own batch ingests nothing,
   because the checkpoint has already consumed the files, so its own reclaim
   correctly refuses too.

`retention.py` behaved correctly at every step it was reached, and its own
printed message even names case (a) and supplies the confirming query. The
control is not broken; it is **unreachable**.

### 16.2 Why this is architectural — the ~48 GB peak is the floor, not a one-off

empresas rejects about one row a month; socios rejects roughly 1,786–1,797.
Both therefore fire the gate **every month, indefinitely**, and neither will
ever self-reclaim through the wired path — only a zero-reject table can reach
reclaim at all. So the peak
[§13.8](#138-for-task-5--the-48-gb-peak) projected — ~48 GB, 2.18× this
Volume's demonstrated high — is not a one-off squeeze against a single
month's upload; it is **the floor for every month F1.4b or a later phase
adds**, against a Volume with no published quota.

Fixing this is **not** in this task — Task 5 is the three ingests — and needs
its own task, most likely with an ADR amendment, because it changes the
storage projection for every month to come. The three hand-deletes recorded
below
([§19](#19-the-hand-delete-between-groups--an-operator-action-not-reclaim-working),
[§21.1](#211-estabs-hand-delete-and-the-phases-running-total)) are an operator
standing in for that missing control, not evidence that it works. The
remaining 2026-06 residue — 5,359,720,597 B of empresas CSVs and 2,852,557,826
B of socios CSVs, 8,212,278,423 B (8.21 GB) together, independently confirmed
in [§21.3](#213-independently-re-confirmed-by-the-controller) — was
**deliberately left in place** rather than hand-cleaned, so the debt this
finding describes stays visible to whoever next reads the Volume instead of
being quietly swept away.

The method that would settle the remaining question — whether `retention.py`
itself deletes correctly once it is actually reached — is to run
`reclaim_landing` through a temporary job resource against an
already-promoted batch, decoupling the test from the gate entirely, the way
Task 1 ran `backfill_snapshot_columns` through a temporary job resource
against the live tables
([§2](#2-what-task-1-changed-and-how-it-deployed-and-ran)). Named as the
method here; deliberately not performed.

---

## 17. Empresas — run `371067950667703`

| task | outcome | start (UTC) | duration |
|---|---|---|---|
| assert_deployed_revision | SUCCESS | 21:17:07 | 29s |
| unzip | SUCCESS | 21:17:42 | 62s |
| ingest | SUCCESS | 21:18:45 | ~5m |
| dq_gate_batch | SUCCESS | — | — |
| check_bad_rows | SUCCESS (false) | — | — |
| fail_on_dq | FAILED, attempts 0 and 1 | — | — |
| promote, reclaim_landing | SKIPPED / EXCLUDED | — | — |

Run state `INTERNAL_ERROR FAILED`, for the reason recorded once, generally, in
[§15.4](#154-fail_on_dq-retried-past-max_retries-0-on-all-three-tables).

**Triage — 1 reject, and it is the same company as last month.**

```
_dq_reject_reason          | rows | rescued_null | rescued_NOT_null
null_or_empty_razao_social |    1 |            1 |                0

cnpj_basico | razao    | natureza_juridica | qualificacao_responsavel | capital_social | porte_empresa | _rescued_data | _snapshot_month | _snapshot_ref_date | src
08314885    | [<NULL>] | 0000              | 00                       | 0,00           | NULL          | NULL          | 2026-07         | 2026-07-11         | K3241.K03200Y2.D60711.EMPRECSV
```

`_rescued_data` is NULL: the parse consumed every field and left nothing over,
so this is source dirt, not a parse defect — the standing test this rule
needed and got. `cnpj_basico` **08314885** is the same CNPJ that was 2026-06's
single reject, with the same reason: a record the RFB has not fixed between
snapshots, not a new event. Persistent source dirt, accepted, and repromoted:

```
$ databricks bundle run repromote_triaged_batch -t free \
    --params table=empresas,batch_id=371067950667703,revision=$(git rev-parse HEAD)
promote_batch: appended 69062849 rows (batch 371067950667703) to workspace.default.bronze_cnpj_empresas
promote_batch: 1 rejected row(s) of batch 371067950667703 stay in quarantine, out of workspace.default.bronze_cnpj_empresas
```

Verification, all by query:

```
_snapshot_month | _snapshot_ref_date | rows     | batches
2026-06         | 2026-06-13         | 68629147 | 1
2026-07         | 2026-07-11         | 69062849 | 1

rows_2026_07 | null_ref_date | min_ref    | max_ref
69062849     | 0             | 2026-07-11 | 2026-07-11

staging_this_batch 69062850 | bronze_this_batch 69062849 | quarantine_this_batch 1
rows 69062849 | with_source 69062849 | distinct_files 10
```

staging = bronze + quarantine **exactly**: 69,062,850 = 69,062,849 + 1. Ten
distinct `_source_file` values, whose per-file row counts sum to 69,062,849 —
every one of the ten parts was read and none was missed. 2026-06's count is
unchanged, so nothing was re-ingested.

---

## 18. Socios — run `409962018634322`

Same shape as [§17](#17-empresas--run-371067950667703): guard SUCCESS, unzip
SUCCESS, ingest SUCCESS, gate fired, `fail_on_dq` FAILED on attempts 0 and 1,
`promote` and `reclaim_landing` `SKIPPED / EXCLUDED`, run `INTERNAL_ERROR
FAILED` by design
([§15.4](#154-fail_on_dq-retried-past-max_retries-0-on-all-three-tables)).

**Triage — 1,786 rejects, all one reason, all `_rescued_data` NULL.**

```
_dq_reject_reason                     | rows | rescued_null | rescued_NOT_null
null_or_empty_nome_socio_razao_social | 1786 |         1786 |                0
```

### 18.1 The mask blinds the triage that reads it

Quarantine masks `nome_socio_razao_social`, so the rule's verdict **cannot**
be confirmed by reading it there — a masked NULL and a masked name are both
`***`. The general point stands beyond this one rule: **a column mask that
protects PII also blinds the triage that reads it**, and the answer is to
verify where the data is unmasked, not to weaken the mask. So the verdict was
reproduced independently against **unmasked staging**:

```
staging rows failing the rule (NULL or trim-empty)     : 1786    <- matches quarantine exactly
staging rows in the whole batch with _rescued_data     : 0
```

Zero rows with `_rescued_data` across the entire 27,992,378-row batch — a
stronger statement than "the rejects parsed cleanly": *nothing* in this batch
had leftover unparsed content. 2026-06 rejected 1,797 for the same reason;
2026-07 rejects 1,786 — source dirt, accepted, repromoted:

```
promote_batch: appended 27990592 rows (batch 409962018634322) to workspace.default.bronze_cnpj_socios
promote_batch: 1786 rejected row(s) of batch 409962018634322 stay in quarantine, out of workspace.default.bronze_cnpj_socios
```

27,992,378 − 1,786 = 27,990,592, predicted before the repromote and matched
exactly.

```
_snapshot_month | _snapshot_ref_date | rows     | batches
2026-06         | 2026-06-13         | 27836651 | 1
2026-07         | 2026-07-11         | 27990592 | 1

rows_2026_07 27990592 | null_ref_date 0 | min_ref = max_ref = 2026-07-11
distinct_files 10 | with_source 27990592
```

**The socios mask, confirmed live in both directions after the ingest** —
which is what makes it evidence rather than a spot check:

```
table_name                    | column_name             | mask_name
bronze_cnpj_socios            | nome_do_representante   | workspace.default.mask_personal_name
bronze_cnpj_socios            | nome_socio_razao_social | workspace.default.mask_personal_name
bronze_cnpj_socios_quarantine | nome_do_representante   | workspace.default.mask_personal_name
bronze_cnpj_socios_quarantine | nome_socio_razao_social | workspace.default.mask_personal_name
(4 rows)   -- masks on any '%staging%' table: 0

SELECT nome_socio_razao_social, nome_do_representante FROM bronze_cnpj_socios  -> ***  ***
SELECT ... FROM bronze_cnpj_socios_staging (batch 409962018634322)             -> real values, len 30, 35
```

Bronze returns `***` to the table owner's own query (`opl_pii_readers` does
not exist, and the mask fails closed); staging returns real values. That is
exactly the asymmetry ADR 0008 argues for: a masked staging would make the
next promote write `***` into bronze permanently, and would make this very DQ
rule evaluate against `***` — neither null nor empty — so it would stop
rejecting.

---

## 19. The hand-delete between groups — an operator action, not reclaim working

With reclaim structurally unreachable
([§16](#16-the-reclaim-finding--eight-instances-two-executions-zero-bytes-ever-deleted))
and the projected peak at 2.18× anything this Volume has held against no
published quota ([§13.8](#138-for-task-5--the-48-gb-peak)), the 2026-07
empresas and socios CSVs were removed **by hand** after their promotes, before
the estabelecimentos run. This is a human doing once what the wired control
cannot reach, and it **must not be read as the retention control working**.

It borrows `opl.bronze.retention`'s own `scope_to_landing_dir` and
`fuse_path` rather than re-spelling them, so the hand action inherits the
control's own refusals — a `zips/` path, a `..`, a backslash — instead of
approximating them. The proof set is the same one `files_of_batch` requires:
`_source_file` for the promoted batch, read from bronze. Dry run first, then
delete:

```
bronze workspace.default.bronze_cnpj_empresas proves 10 file(s) for batch 371067950667703
scoped to /Volumes/workspace/default/landing/cnpj/2026-07/empresas
  INSIDE  .../K3241.K03200Y0.D60711.EMPRECSV        (x10, no REFUSED entries)
  before: 10 file(s), 5,394,092,286 B
  deleted 10 file(s), 5,394,092,286 B freed, 0 failure(s)
  after: 0 file(s), 0 B
```

Socios:

```
  before: 10 file(s), 2,868,482,991 B
  deleted 10 file(s), 2,868,482,991 B freed, 0 failure(s)
  after: 0 file(s), 0 B
```

**8,262,575,277 B freed, 0 failures, nothing under `zips/` touched** — which is
what keeps this reversible: the CSVs are reconstructible by re-running
`unzip`, so the cost of this delete is a re-unzip and nothing else. Footprint
afterward:

```
22,849,195,610  TOTAL (22.85 GB)
```

— within 105,766 B of
[§13.1](#131-step-0--the-headroom-and-no-quota-figure-exists)'s post-Task-4
baseline of 22,849,089,844 B, the difference being the new month-scoped
checkpoint state. So the estabelecimentos run below went against a Volume
back at the Task-4 baseline, projecting a peak near **39.7 GB** instead of the
~48 GB [§13.8](#138-for-task-5--the-48-gb-peak) forecast — 1.80× the
historical high rather than 2.18×.

---

## 20. Estabelecimentos — run `128878829411613`

| task | outcome | start (UTC) |
|---|---|---|
| assert_deployed_revision | SUCCESS | 21:42:05 |
| unzip | SUCCESS | ~21:42:4x (10 files, 16,860,178,427 B, all `D60711`) |
| ingest | SUCCESS | — |
| dq_gate_batch | SUCCESS | — |
| check_bad_rows | SUCCESS (false) | — |
| fail_on_dq | FAILED, attempts 0 and 1 | — |
| promote, reclaim_landing | SKIPPED / EXCLUDED | — |

Run state `INTERNAL_ERROR FAILED`, same design as
[§15.4](#154-fail_on_dq-retried-past-max_retries-0-on-all-three-tables).

### 20.1 Task 1's migration proven, by the event it was built for

[§1](#1-why-bronze_cnpj_estab_quarantine-needed-migrating-at-all) probed two
hazards before Task 1 ran: a target with fewer columns than an incoming frame
(the quarantine hazard), and a target with more (the repromote hazard). This
run exercised the first one for real, in both halves of what Task 1 changed:

```
version | operation
0       | CREATE TABLE AS SELECT     <- the gate's original empty-frame create
1       | ADD COLUMNS                <- Task 1's migration
2       | WRITE                      <- FIRST write commit EVER into estab quarantine
```

The 37-column stream appended into 37-column staging without a schema error —
the failure [§1](#1-why-bronze_cnpj_estab_quarantine-needed-migrating-at-all)
predicted would "stop the next ingest outright" if the migration had not run.
And then the half that had never been exercised at all: a 38-column reject
frame appended into the 38-column quarantine Task 1 built, with no
`mergeSchema` anywhere. Before this run that table had 0 rows and no write
commit in its entire history
([§6](#6-the-quarantine-run),
[§10](#10-cleanup-reverted-and-redeployed--confirmed-in-the-deployed-state)).
**Task 1's fix is complete, and this is the evidence — the first estab
rejects in project history are also the first thing to test it.**

### 20.2 The four rejects, and the triage-doctrine correction

```
_dq_reject_reason         | rows | rescued_null | rescued_NOT_null
encoding_replacement_char |    4 |            4 |                0
```

**This is the document's most consequential finding.** The controller's own
dispatch instruction for triage read: for each rejected row, is
`_rescued_data` null? If it is not, the parse is wrong and that is a defect,
not source dirt. All four of these rejects have `_rescued_data` **NULL** — and
are, nonetheless, a parse defect. Following the instruction as written would
have repromoted four rows with lost bytes into bronze.

The reason is what the reject reason names. `_rescued_data` NULL says the CSV
**field-splitting** consumed every field and left nothing over; it says
nothing about **character decoding**, which happens independently — so for
this one rule the standing test is necessary but not sufficient.
`CSV_DIALECT["encoding"]` is `cp1252`. windows-1252 leaves five byte values
undefined (`0x81 0x8D 0x8F 0x90 0x9D`); Java's decoder substitutes U+FFFD for
them **silently**, where Python raises. A U+FFFD here means a source byte was
**lost**, not that a wrong codepage was applied. Nothing is *rescued*, because
the parse succeeded — lossily.

Which column carries it was, in
[ADR 0006](adr/0006-bronze-dq-gate-policy.md)'s own words, "not yet known". It
is now:

```
cnpj_basico | email_with_marker | len | pos
02546225    | [{FFFD}]          |   1 |   1
02586244    | [{FFFD}]          |   1 |   1
02631716    | [{FFFD}]          |   1 |   1
02653802    | [{FFFD}{FFFD}]    |   2 |   1
```

**`correio_eletronico`, in all four — and the field's entire content is the
undecodable byte(s).** There is no truncated address and no real email being
lost: the source field held one or two bytes that are not cp1252 and nothing
else. Every other column of all four rows parsed cleanly. Verdict: source
contamination, correctly detected — ADR 0006 already ruled out repair
("guessing a codepage per record would be inventing data"), so the four stay
in quarantine.

**State the correction as doctrine, not as an incident: "`_rescued_data` is
NULL" does not mean "source dirt"; the triage question is per reject reason,
not universal.** The instruction to check `_rescued_data` came from the
controller's own dispatch; the implementer went past it, checked the charset
and the byte set, and named the column instead of applying the rule
literally. A document that records only the code's defects and not the
instructions' is not an honest record, so this correction is recorded here as
a correction to the *dispatch*, and folded into
[ADR 0006](adr/0006-bronze-dq-gate-policy.md) as the answer to the column
question it had left open.

### 20.3 The cross-month asymmetry, and its F2 consequence

`correio_eletronico` is not one of the two columns the encoding check covered
when 2026-06 was gated. Timeline, from git:

- `ed004e0` (2026-07-24) — estab rules with the encoding check over
  `nome_fantasia` and `logradouro` only;
- **2026-06 estab run `118868417561350`, 2026-07-28** — gated by that narrow
  check, rejected zero;
- `caed88e` (2026-07-31) — widened the check to U+FFFD over every column,
  which is what caught these four.

So the widened check retroactively implies un-flagged rows in 2026-06's
bronze. It does, exactly:

```
_snapshot_month | rows_with_fffd_in_email        2026-06 | cnpj_basico | cnpj_ordem | len
2026-06         | 4                                      | 02546225    | 0001       |   1
                                                          | 02586244    | 0001       |   1
                                                          | 02631716    | 0001       |   1
                                                          | 02653802    | 0001       |   2
```

The same four companies, the same email lengths — present in 2026-06's
bronze, promoted un-flagged, and they are 2026-06's *only* U+FFFD rows across
every free-text column checked. **This is a rule change, not a source
change**: the encoding check covered 2 of 30 columns until `caed88e`, after
the 2026-06 run, and `promote_batch` re-applies the current rules at promote
time rather than the rules a row was first gated under.

Deciding whether these four belong in bronze is not the operator's call, and
the mechanism enforces that rather than merely stating it: `promote_batch`
re-applies the DQ rules and appends only what passes —

```
promote_batch: appended 72318964 rows (batch 128878829411613) to workspace.default.bronze_cnpj_estabelecimentos
promote_batch: 4 rejected row(s) of batch 128878829411613 stay in quarantine, out of workspace.default.bronze_cnpj_estabelecimentos
```

— so the four stayed out of 2026-07's bronze by construction, while their
2026-06 counterparts remain in 2026-06's bronze, unflagged, because they were
promoted before the rule that would now catch them existed.

**The F2 consequence, stated plainly, because it is a modelling hazard and
not a curiosity:** for these four establishments, a satellite will see a
2026-06 row and no 2026-07 row, and will read that as "unchanged" when the
truth is "the 2026-07 observation was quarantined". **A satellite cannot
distinguish unchanged from not-observed unless something records the
difference.** This belongs in the F2 handoff, not only here.

Verification:

```
_snapshot_month | _snapshot_ref_date | rows     | batches
2026-06         | 2026-06-13         | 71874448 | 1
2026-07         | 2026-07-11         | 72318964 | 1

rows_2026_07 72318964 | null_ref_date 0 | min_ref = max_ref = 2026-07-11
staging_this_batch 72318968 | bronze 72318964 | quarantine 4     (72,318,968 = 72,318,964 + 4)
rows 72318964 | with_source 72318964 | distinct_files 10
```

Zero rows with `_rescued_data` in the whole 72,318,968-row staging batch.

---

## 21. Final state, row counts, and independent verification

### 21.1 Estab's hand-delete, and the phase's running total

Estab's CSVs were hand-reclaimed on the same terms as
[§19](#19-the-hand-delete-between-groups--an-operator-action-not-reclaim-working)
— 10 files proven in bronze first, the same `scope_to_landing_dir` scoping, no
`REFUSED` entries:

```
before: 10 file(s), 16,860,178,427 B    deleted 10, 16,860,178,427 B freed, 0 failure(s)    after: 0 file(s), 0 B
```

Total hand-freed this phase: **25,122,753,704 B** — which is, to the byte,
[§13.8](#138-for-task-5--the-48-gb-peak)'s predicted "2026-07 unzipped"
figure. Final footprint **22,849,249,018 B (22.85 GB)** — back to the Task-4
baseline; the observed peak across the phase was **39.71 GB** (1.80× the
historical high) rather than the forecast 47.97 GB (2.18×).

All three of these were operator actions standing in for a control that
cannot execute
([§16](#16-the-reclaim-finding--eight-instances-two-executions-zero-bytes-ever-deleted)),
never reclaim itself, and each was scoped through `retention.py`'s own
refusals rather than an approximation of them. Nothing under `zips/` was
touched by any of the three, which is what keeps all of it reversible by
re-running `unzip`.

Final checkpoint/schema state, unchanged in the ways that matter:

```
_checkpoints/  -> 2026-07  bronze_cnpj_empresas  bronze_cnpj_estab  bronze_cnpj_lookup  bronze_cnpj_socios
_checkpoints/2026-07/ -> bronze_cnpj_empresas  bronze_cnpj_estab  bronze_cnpj_socios
_schemas/      -> same shape          _schemas/2026-07/ -> the same three
```

Old unscoped dirs after all three runs — every offset and commit still
pre-dating this phase's runs:

```
bronze_cnpj_empresas offsets 2026-08-01T14:07:04-05  commits 2026-08-01T14:11:16-18
bronze_cnpj_socios   offsets 2026-08-01T14:16:09     commits 2026-08-01T14:17:43-44
bronze_cnpj_estab    offsets 2026-07-28T16:51:56-57  commits 2026-07-28T16:56:54-57
```

`HEAD` is still `534ea92` with a clean tree: nothing was committed between the
deploy and the last run, so every launch ran against the wheel
[§15.1](#151-deploy-verified-by-artefact-rather-than-by-its-success-line)'s
guard verified.

### 21.2 Row counts, all three tables, both months

| table | 2026-06 | 2026-07 | delta | rejects 2026-06 | rejects 2026-07 |
|---|---|---|---|---|---|
| empresas | 68,629,147 | 69,062,849 | +433,702 | 1 | 1 |
| socios | 27,836,651 | 27,990,592 | +153,941 | 1,797 | 1,786 |
| estabelecimentos | 71,874,448 | 72,318,964 | +444,516 | 0 (narrow check) | 4 |

Every 2026-06 figure is unchanged from the pre-run baseline in
[§15.2](#152-pre-run-baselines-taken-before-anything-ran), so nothing was
re-ingested. `_snapshot_ref_date` is **2026-07-11** for all three — not
month-end, exactly the `D60711` the RFB declares in the inner filenames.

### 21.3 Independently re-confirmed by the controller

Everything below was queried directly against the warehouse, separately from
the implementer's own report, after all three ingests and both hand-deletes —
included here as a second, independent source for facts the sections above
already established, plus two items the report did not check.

| table | 2026-06 rows / ref | 2026-07 rows / ref | distinct `_source_file` |
|---|---|---|---|
| empresas | 68,629,147 @ 2026-06-13 | 69,062,849 @ 2026-07-11 | 10 each |
| socios | 27,836,651 @ 2026-06-13 | 27,990,592 @ 2026-07-11 | 10 each |
| estabelecimentos | 71,874,448 @ 2026-06-13 | 72,318,964 @ 2026-07-11 | 10 each |

- Reconciliation for empresas 2026-07: staging 69,062,850 = bronze 69,062,849
  + quarantine 1, exactly.
- `bronze_cnpj_estab_quarantine` reject breakdown: `encoding_replacement_char`,
  4 rows, all 4 with `_rescued_data` NULL.
- `DESCRIBE HISTORY bronze_cnpj_estab_quarantine`: v0 `CREATE TABLE AS
  SELECT`, v1 `ADD COLUMNS`, v2 `WRITE`.
- Joining the quarantined establishments back to bronze on
  `cnpj_basico`/`cnpj_ordem`/`cnpj_dv` — a different method from the report's
  own retroactive query — returns 4 rows, `_snapshot_month = 2026-06` only:
  they are in 2026-06's bronze and absent from 2026-07's.
- `column_masks` after the ingests lists only `bronze_cnpj_socios` and
  `bronze_cnpj_socios_quarantine`, two columns each; nothing on any
  `%staging%`. A `SELECT` as the table owner returns `***` from bronze and a
  real value (length 30) from staging.
- `_checkpoints/` holds `2026-07/` beside the four unscoped `<table_key>`
  dirs and inside none of them; `_schemas/` the same; **no `2026-06/`** under
  either; `_checkpoints/2026-07/` holds `bronze_cnpj_empresas`,
  `bronze_cnpj_socios`, `bronze_cnpj_estab`.
- Landing after the hand-deletes: `2026-07/empresas` and `2026-07/socios` at 0
  files; **`2026-06/empresas` 10 files / 5,359,720,597 B and `2026-06/socios`
  10 files / 2,852,557,826 B still present** — 8,212,278,423 B (8.21 GB)
  together, the residue named in
  [§16.2](#162-why-this-is-architectural--the-48-gb-peak-is-the-floor-not-a-one-off).

Every one of these agrees with what the implementer's own report recorded
live; nothing above changes any number given in §15–§21 — it is a second,
separately-taken source for the same facts, plus the exact 2026-06 landing
byte counts the report did not print.

---

## 22. What Task 5 hands forward

**For Task 3 — per-table per-month reject rates:**

| table | 2026-06 | 2026-07 | reason(s) |
|---|---|---|---|
| empresas | 1 / 68,629,148 = 1.5e-8 | 1 / 69,062,850 = 1.4e-8 | `null_or_empty_razao_social`, the same CNPJ both months |
| socios | 1,797 / 27,838,448 = 6.5e-5 | 1,786 / 27,992,378 = 6.4e-5 | `null_or_empty_nome_socio_razao_social` |
| estabelecimentos | 0 / 71,874,448 (narrow check) | 4 / 72,318,968 = 5.5e-8 | `encoding_replacement_char` in `correio_eletronico` |

The estab pair is **not comparable across months** — the rule set widened
between the two runs
([§20.3](#203-the-cross-month-asymmetry-and-its-f2-consequence)), so 2026-06's
zero measures a 2-column check and 2026-07's four measure a 30-column one.
Rates are otherwise stable to one significant figure.

**For Task 6 — two months of estabelecimentos now exist**: `2026-06`
71,874,448 and `2026-07` 72,318,964, one `_batch_id` each, distinct
`_snapshot_ref_date`s (2026-06-13 / 2026-07-11) — the multi-month property F2
depends on, asserted rather than assumed. One caveat for any month-over-month
diff: the four `correio_eletronico` records in
[§20.3](#203-the-cross-month-asymmetry-and-its-f2-consequence) are present in
2026-06 and absent in 2026-07 for a rule reason, not a source reason.

**For Task 7:**

1. `reclaim_landing` is unreachable
   ([§16](#16-the-reclaim-finding--eight-instances-two-executions-zero-bytes-ever-deleted))
   and is this phase's largest finding. It needs its own task, and probably an
   ADR amendment, because it changes the storage projection for every month
   added.
2. ADR 0006's encoding item is updated in this same round — see the addition
   in [ADR 0006](adr/0006-bronze-dq-gate-policy.md) — with "which column
   holds it" now answered. Its other half stays outstanding: folding the
   check into a per-column metric rather than only a reject reason.
3. **The 2026-06 backfill question this raises.** 2026-06's bronze holds four
   rows the current rule set would reject. Whether the system of record
   should be re-gated when a rule widens is a policy question this phase
   surfaced and did not answer.
